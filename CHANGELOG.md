# Changelog

All notable changes to SpeedKalandra are tracked here. The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and the project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

Each release section is short and user-facing; engineering rationale lives next to the code (or in `ARCHITECTURE.md` when it shapes the design). Pre-release `v17.x` tags are listed at the end of this file as historical context only; they are no longer kept in source comments.

## [Unreleased]

### Changed

- **Os layouts experimentais "Plus" viraram canônicos e a flag de opt-in foi removida.** O par Classic/Plus existia desde a primeira versao dos widgets sob a flag `cfg.layoutVariant` (default `classic`, `plus` opt-in BETA). Na pratica os Plus passaram meses sendo a versao usada — mono timer, reflow percentual, resize por borda — e o Classic ficou como fallback que ninguem mais consultava. A duplicacao gerava custo permanente (toda mudanca visual passava por dois caminhos, e a if/else baseada em `layoutVariant` no composition root crescia a cada feature). Esta release promove os widgets Plus para canonicos: os tres `*_layout_widget.ahk` (compact / micro / steve) agora carregam a logica Plus diretamente, sem ramificacao, e os arquivos `*_layout_plus_widget.ahk` (separados ate aqui) foram absorvidos.

  Com os Plus canonicos, o flag `cfg.layoutVariant` perde sentido e foi removido junto com a secao INI `[Layouts]`, o checkbox `LAYOUTS (BETA)` em Settings, e o MsgBox "reinicie para aplicar" que acompanhava a troca de variante. Documento `PLUS_LAYOUTS_SPEC.md` apagado — o que era spec virou comportamento padrao.

  **Back-compat.** INIs antigos com `[Layouts] Variant=plus` (ou `=classic`) continuam carregando sem erro: a secao vira chave orfa silenciosa (na linha das outras chaves orfas `PanelOverlayKeys` / `GamePauseDetectionEnabled` que o repository ja documentava). O proximo `Save()` nao reescreve `[Layouts]`, entao a secao desaparece organicamente do disco — sem pass de migracao dedicada. Usuario que estava em `classic` migra para o comportamento Plus (que agora se chama apenas "layout") sem aviso porque os Plus sao os que ele provavelmente esperava ver de qualquer jeito; nenhum atalho de tecla muda; geometrias persistidas em `[Overlay]` continuam validas porque os IDs dos widgets (`compactLayout` / `microLayout` / `steveLayout`) sao os mesmos.

  ToS compliance (GSG §18): refactor puramente de UI/persistencia. Zero leitura de game files novos, zero simulacao de input, zero rede. **No risk** under PoE2 Terms of Use clauses 7(b–f, i). GSG §17 anti-regression review: 24 items revisados, nenhum violado; em particular **item 1 (hydrate-before-subscribers)** preservado (a remocao das chamadas `_LoadLayouts` / `_SaveLayouts` em `SettingsRepository` nao afeta ordem de hidratacao), **item 2 (round-trip de persistencia)** preservado (toda secao restante salva e carrega identicamente; chaves orfas legacy nao quebram o boot, testes de round-trip por secao continuam passando), e **item 11 (resize-by-border)** preservado (o resize por borda dos widgets ja era comportamento Plus e agora e o canonico). Suite mantem o mesmo numero de testes uteis: 6 testes que afirmavam o comportamento ramificado por `layoutVariant` (4 em `app_settings_tests`, 2 em `settings_repository_tests`) foram removidos; 9 testes mortos de Compact Classic em `compact_layout_widget_tests` removidos; 14 testes novos cobrindo os static helpers dos widgets agora canonicos (extraidos durante a promocao para reduzir o tamanho dos metodos render). Comentarios espalhados pelo codigo que referenciavam "Steve Plus" / "Compact Plus" / "Compact Classic" / `PLUS_LAYOUTS_SPEC.md` foram apagados ou reescritos.

- **`MAX ACT FILTER` no plot de Run Statistics passa a cortar dados acima do ato, não filtrar runs inteiras.** O dropdown vinha rotulado como "MAX ACT FILTER" mas implementava semântica de **min act**: `Act 3+` significava "incluir apenas runs cujo `maxActReached >= 3`" (filtra runs inteiras pelo que cada uma alcançou), e o filtro só afetava o line chart — KPIs do header, total no header e barra "Current run distribution" mostravam a run completa. Resultado: ao escolher `Act 1+` numa run que chegou ao meio do Ato 2, o tempo gasto no Ato 2 parcial continuava aparecendo em todas as superfícies da view. A semântica nova é "cortar dados acima do ato N": ao escolher `Act 2`, o plot inteiro (KPIs, current-run bar, line chart) reflete apenas dados de Acts 1 e 2; tempo gasto em Acts 3+ é descartado da view (não dos dados em disco, que continuam intactos). Runs históricas com `maxActReached < N` continuam aparecendo no chart — mostram só o que têm, em vez de serem excluídas.

  Mecanismo: novo método estático `RunStatsPlotBuilder.FilterByMaxAct(data, maxAct)` retorna uma cópia do `data` com `details` filtrado por ato (extraído do `note` via regex `(?:Ato|Act)\s+(\d+)`, aceitando ambos os formatos legacy/atual), e `totals`/`totalMs` recalculados sobre os details retidos. Três políticas de over-include preservam dados que o filtro não consegue ancorar com confiança: (a) details com `category=morte` sempre passam (mortes não carregam ato no snapshot atual — ver BACKLOG B2 pra trilha da feature que adicionaria), (b) details cujo `note` não parseia como ato sempre passam (zonas fora do catálogo, runs legacy), (c) `maxActReached` no result Map preserva o valor original — descreve a run, não a view filtrada. Helpers `_DetailPassesMaxAct` (predicate) e `_ShallowCloneData` (clone para evitar mutação do input — o dialog cacheia o build unfiltered e reaplica o filtro a cada mudança de dropdown).

  Loadings agora carregam `note: "Act N"` derivado da `toZone` via catálogo, dentro de `_AddLoadingDetails`. A atribuição pela zona de destino é semanticamente correta — um loading **para** o Ato 2 pertence ao orçamento do Ato 2, não do Ato 1. Loadings cuja `toZone` não está no catálogo ficam com `note=""` e caem no over-include. Efeito colateral: o granularity "By act" do line chart agora inclui loadings no total de cada ato (mais preciso — o tempo "do Ato 1" deveria incluir loadings durante o Ato 1; antes ficavam de fora).

  Lista do dropdown: `["All", "Act 1", "Act 2", "Act 3", "Act 4", "Interlude"]` (era `["All", "Act 1+", ..., "Act 10+"]`). Acts 5-10 nunca existiram em produção — o catálogo (`data/zones.csv`) tem só Acts 1-4. "Interlude" é placeholder: hoje mapeia pro sentinel `maxAct=999` que funciona como no-op (igual a "All"), porque o tracker não distingue cruel/interlude do normal — esse trabalho está no `BACKLOG.md` item B1 (cruel/interlude tracking pipeline). O sentinel é validado por teste pra que uma futura refactor não rompa o contrato silenciosamente. Sinal `+` removido do label conforme pedido.

  Aplicação consistente: `RunStatsPlotDialog._ShowWithData` aplica `FilterByMaxAct(data, this._maxActFilter)` ao current data ANTES de `_BuildGui`, então KPIs/header total/current-run bar refletem o filtro. `_CollectRunsForChart` aplica `FilterByMaxAct` a cada run histórica carregada (já não há mais filtro "drop run if maxActReached < N"), e a política de carga muda: quando o filtro está ativo (`maxAct > 0`), sempre carrega o INI completo da run (não o summary), porque o summary só tem `totals` pré-agregados e não permite recortar por ato post-hoc. Quando o filtro é "All", o summary continua sendo o caminho rápido sob granularity "run" — preserva a performance da rota mais comum.

  Renames: `MIN_ACT_LABELS` → `MAX_ACT_LABELS`, `_minActFilter` → `_maxActFilter`, `_OnMinActFilterChanged` → `_OnMaxActFilterChanged`, ctrl key `"dropdown_min_act"` → `"dropdown_max_act"`. Dois helpers novos `_MaxActFilterValueFromIndex` / `_MaxActFilterIndexFromValue` mantêm o mapeamento idx↔value simétrico entre o handler e o `Choose<N>` inicial (`idx 1 → 0 All`, `idx 2..5 → 1..4 Acts`, `idx 6 → 999 Interlude`).

  ToS compliance (GSG §18): mudança puramente de UI/agregação. Zero leitura de game files novos, zero simulação de input, zero rede. **No risk** under PoE2 Terms of Use clauses 7(b–f, i). GSG §17 anti-regression review: 24 items revisados, nenhum violado; em particular, `maxActReached` continua sendo derivado e persistido do mesmo jeito (o filtro só passa a usar pra outra coisa), a round-trip de persistência não é tocada, o schema do INI não muda, e os legacy notes `"Ato N"` continuam aceitos pelo regex de extração. 17 testes novos em `run_stats_plot_builder_tests.ahk`: 3 cobrem o `note` de loadings (atribuição via `toZone` no catálogo, vazio quando a zona é desconhecida, vazio quando `toZone` está ausente), e 14 cobrem `FilterByMaxAct` (no-op em `maxAct=0` e negativo, drop de details acima do max, retenção no boundary `act == maxAct`, mortes bypassam, details sem ato parseável bypassam, loadings com act ≤ maxAct sobrevivem, loadings com act > maxAct caem, totals/totalMs recalculados, `maxActReached` e metadata preservados, input não-mutado, e o sentinel 999 do Interlude como no-op).

- **Unified layout control: 3 hotkey actions → 1 `CycleLayout`, `ToggleOverlay` removed.** The previous `ToggleMicroLock` / `ToggleSteveLock` / `ToggleOverlay` trio collapsed into a single `CycleLayout` action that walks `STEVE → COMPACT → MICRO → STEVE` in a fixed loop. The two `Toggle*Lock` actions gave the user two separate hotkeys for the same conceptual choice ("pick a layout"); a speedrunner usually picks a layout pre-run and stays in it, and the rare mid-session swap is now one repeated keypress instead of two competing bindings. `ToggleOverlay` (hide/show overlay) was removed entirely — the hover-dim feature in `OverlayInteractionService` already drops opacity to ~10% when the mouse passes over the overlay, which covers the underlying "the overlay is in the way of an item drop" need without the cognitive load of a hide-state the user has to remember to toggle back.

  Default hotkey for `CycleLayout` is `^F8` (the slot `ToggleSteveLock` used to occupy by default) so a fresh install lands on a working bind. Returning users keep their muscle memory via migration in `SettingsRepository._LoadHotkeys`: the legacy `ToggleSteveLock` bind takes precedence over `ToggleMicroLock`, both override only when `CycleLayout` is absent from the INI (an explicit user bind always wins), and an empty legacy bind never shadows the default — a user who cleared both lock toggles in the old Settings dialog doesn't silently lose the new `^F8`. `ToggleOverlay` is discarded silently from any INI it appears in. On the next Save, `_SaveHotkeys` writes only `CycleLayout` (the `_SyncMapSection` helper already removes obsolete keys), so the legacy entries disappear from disk without a separate cleanup pass.

  Touches: `Commands` (three constants out, `CycleOverlayLayoutRequested` in; `SetOverlayModeRequested` retained as the programmatic API), `HotkeyService.ActionToCommand` (9 → 7 entries), `OverlayModeService` (replaced `ToggleMicroLock` / `ToggleSteveLock` methods with a single `CycleLayout`; the `_SyncWindowFlags` invariant — mutually exclusive lock flags persisted into `cfg.window` — is preserved so the Hydrate path still reconstructs the right mode on the next boot), `SpeedKalandraApp.ToggleOverlay()` (deleted along with its `_WireEventHandlers` subscribe), `AppSettings._DefaultHotkeys` (9 → 7), `SettingsRepository._LoadHotkeys` (migration + ToggleOverlay drop). The `Evt.OverlayModeChanged{mode, prevMode, locked, steveLocked}` payload shape is unchanged — downstream subscribers (notably the persistence path that mirrors locked flags into `cfg.window`) keep working without edits.

  ToS compliance (GSG §18): pure overlay/hotkey refactor. Zero game-file reads, zero input simulation, zero network. **No risk** under PoE2 Terms of Use clauses 7(b–f, i). GSG §17 anti-regression review: all 24 items reviewed, none violated; in particular, hotkey persistence stays in `[Hotkeys]` with the same Save-removes-obsolete behaviour, `OverlayModeService` still subscribes BEFORE `Hydrate` is called from the composition root, no FIFO-dependent flows touched, and the cycle handler stays a pure state machine with no GUI side effects (the TrayTip is silent on Mute, matching the previous `_OnSetModeRequested` behaviour). ~17 new/changed tests across four files: `overlay_mode_service_tests.ahk` rewritten end-to-end (10 cycle tests covering every transition plus the mutually-exclusive-flags invariant, 2 command-subscriber tests for `CycleOverlayLayoutRequested`); `hotkey_service_tests.ahk` updated for the 9→7 collapse plus a new `cycle_layout_maps_to_cycle_command` lock test; `app_settings_tests.ahk` updated (`defaults_hotkeys_includes_seven_actions` pins both the count and the explicit absence of the three removed actions); `settings_repository_tests.ahk` adds 7 migration tests — each legacy bind migrating individually, Steve precedence, explicit CycleLayout wins, ToggleOverlay discarded, empty legacy ignored, and a full round-trip proving the legacy keys don't resurrect on disk after a Save.

### Added

- **Cruel/Interlude tracking pipeline: per-(act, stage) PBs + Interlude filter promovido a real (B1 Commits 1-5 of 5).** A run de speedrun PoE2 EA é "campanha (Atos 1-4) + interlúdio (Atos 1-4 em cruel difficulty)", mas o tracker pré-B1 NÃO distinguia cruel de normal: o mesmo `Clearfell Encampment` no Ato 1 normal e no Ato 1 do interlúdio aparecia como `act=1` em ambos, e o pipeline `LogMonitorService → ZoneTrackingService → ActCheckpointTracker → PersonalBestService → RunHistoryRepository → RunStatsPlotBuilder` colapsava as duas timings num único bucket per-act. Consequências silenciosas: (a) o checkpoint do Act 1 normal era sobrescrito quando o jogador entrava em qualquer zona cruel do Act 1 (last-write-wins num `Map<actInt, runMs>`), (b) `PersonalBestService.GetRunPbForAct(1)` somava normal + cruel num único per-act PB — comparação injusta entre runs full-campaign e runs só-normal, (c) o filtro "Interlude" no plot dialog (introduzido no commit anterior do Max Act como placeholder do sentinel `999`) só funcionava como no-op porque não havia dado de stage pra cortar, (d) o detail Map do builder emitia `note: "Act N"` sem distinção de stage, então um futuro filtro genuíno também não teria como decidir. B1 fecha o gap em 5 commits incrementais, todos com suite verde entre commits.

  **Commit 1 — LogMonitor cruel-aware (Layer A).** PoE2 emite cruel zone gens via a MESMA linha `Generating level N area "<code>"` mas com prefixo `C_` no code (e.g. `C_G1_3`). Empiricamente verificado contra um Client.txt real com milhares de transições cruel: o engine **NÃO** emite `[SCENE] Set Source [...]` correspondente em cruel — só a linha area-gen. Consequência: se o serviço só publicasse `ZoneChanged` pelo branch `[SCENE]`, todo o pipeline downstream (zone tracker, route widget, active-zone display, checkpoints) ficaria cego durante o interlúdio inteiro. Solução: o branch area-level passou a double-publish em cruel — `AreaLevelChanged` (como antes) **E** `ZoneChanged` com `stage: "interlude"`. Zonas normais continuam deferindo o `ZoneChanged` pro `[SCENE]` subsequente, agora carregando `stage: "normal"` por default. Resolução do nome humano: o code `C_<base>` é stripped pelo prefixo `CRUEL_PREFIX` (static class constant), depois resolvido via catálogo — então `C_G1_3` vira `"Mud Burrow"` no payload e `"C_G1_3"` fica preservado em `sceneId` pra diagnóstico. Todos os caminhos de `ZoneChanged` (area-gen cruel, [SCENE], "You have entered") agora carregam `stage` explícito. +10 unit tests em `log_monitor_service_tests.ahk`.

  **Commit 2 — Domain stage propagation.** `ZoneTrackingService._OnZoneChanged` passou a ler `stage` do payload (default `"normal"`, string vazia também cai pro default) e propagá-lo no `ZoneEntered` re-published downstream. `ActCheckpointTracker` ganhou campos paralelos: `_currentStage` (string) e `_checkpointsByActStage` (`Map<"act|stage", runMs>`) coexistindo com o legacy `_checkpointsByAct` (`Map<actInt, runMs>`) que preserva o comportamento last-write-wins pra callers ainda não migrados. Detecção de transição passou de `newAct != _currentAct` pra `newAct != _currentAct OR newStage != _currentStage` — uma entrada em cruel-Act-1 vindo de normal-Act-2 dispara captura do checkpoint `"2|normal"`. Helper estático `_ComposeKey(act, stage)` garante schema consistente da chave composite (`Integer(act) . "|" . stage`). API nova: `GetCurrentStage()` e `GetCheckpointsByStage()`. +20 unit tests em `zone_tracking_service_tests.ahk` e `act_checkpoint_tracker_tests.ahk`.

  **Commit 3 — Per-(act, stage) PB persistence.** Schema do `data/personal_bests.ini` ganhou keys `Act<N>NormalMs` e `Act<N>InterludeMs` no lugar do legacy `Act<N>Ms`. `PersonalBestRepository.Load` aceita **ambos** os formatos: chave legacy `Act<N>Ms` é interpretada como stage "normal" automaticamente (migração transparente sem pass dedicada), e o Load retorna AMBAS as projeções (`runPbByActStage` canônica + `runPbByAct` legacy via filtro normal-only) pra coexistência. `PersonalBestService` ganhou API nova `GetRunPbForActStage(act, stage)` / `HasRunPbForActStage(act, stage)` / `GetAllRunPbsByActStage()` enquanto preserva os aliases legacy `GetRunPbForAct(N)` etc — pelo helper `_NormalizeCheckpointKey(rawKey)` que aceita transparentemente chaves integer, composite-string ou numeric-string vindas de qualquer caller histórico. Mesmo tratamento simétrico em `RunHistoryRepository`: a section `[checkpoints]` do INI da run migra pra `Act<N>NormalMs` / `Act<N>InterludeMs`, ambos formatos aceitos no Load, Map retornado sempre composite-keyed. `RunSnapshotSaver._captureCheckpoints` agora chama `GetCheckpointsByStage()` em vez do legacy. `RunAverageService` aceita ambos os formatos no input mas a API integer-keyed retorna só o stage normal (legacy semantics preservada). 20 unit tests novos cobrindo round-trip por (act, stage), migração de chaves legacy, e a nova "B1 Layer B" de 16 testes em `personal_best_service_tests.ahk`.

  **Commit 4 — Plot builder stage propagation + Interlude filter real.** `RunStatsPlotBuilder` ganhou suporte forward-compatible a stage no snapshot input: quando `zoneTotalsByStage` (composite `"zone|stage"`) está presente, cada entry vira um detail com o `stage` correspondente; caso contrário, fallback pra `zoneTotals` legacy com todos os entries default-ando pra `"normal"`. Loading events ganharam campo opcional `stage` (default `"normal"` quando ausente, fallback defensivo pra `"normal"` se valor inválido). Mortes carregam `stage: ""` porque o detail agregado spans both stages por definição (single sum) — a categoria `morte` é bypass de filter pela categoria, não pelo stage. Detail Map ganhou um sexto campo `stage` (não no `note`, pra não colidir com o parsing existente). `FilterByAct(999)` (o sentinel "Interlude" do dialog dropdown) parou de ser no-op e passou a filtrar de verdade: mantém só `stage == "interlude"` entries, com mortes bypassando via categoria como antes. `FilterByAct(1..4)` ganhou guarda adicional `stage == "normal"` — Interlude entries com o mesmo string `"Act N"` no note são rejeitados porque o user picou o ato da campanha, não o ladder cruel. Pre-B1 runs (sem stage nos details) tratam stage ausente como "normal" no `_DetailPassesAct`, mantendo per-act filter funcionando em data legacy. Run histórica que nunca trackeou cruel vira plot vazio sob filtro Interlude — honest result, not data loss. +13 unit tests novos em `run_stats_plot_builder_tests.ahk` cobrindo stage propagation, FilterByAct stage semantics, e o `Interlude_sentinel` reescrito (era no-op, agora filtra). Comentários do `RunStatsPlotDialog` atualizados — o sentinel 999 deixou de ser placeholder e o ACT_FILTER_LABELS header agora documenta a semantic real.

  **Commit 5 — End-to-end integration test.** Novo arquivo `tests_v2/integration/b1_cruel_pipeline_integration_tests.ahk` exercitando 3 cenários através do composition root real (`SpeedKalandraApp` headless): (1) cruel area-gen line via `logMonitor.ProcessText` produz `ZoneChanged{stage: "interlude", zoneName: "Mud Burrow", sceneId: "C_G1_3"}` e o `ZoneEntered` re-published pelo `ZoneTrackingService` preserva o stage + actNum + isTown enriched do catálogo; (2) cenário normal→cruel mid-run captura dois checkpoints independentes no `actCheckpoints.GetCheckpointsByStage()` (`"1|normal"=60000`, `"2|normal"=150000`) com o bucket `"1|interlude"` ainda ativo até o finalize; (3) finalize completa o ciclo: `PersonalBestService` tem 3 buckets via `GetRunPbForActStage`, legacy `GetRunPbForAct(N)` projeta normal-only, e ambos o PB INI e o history INI fazem round-trip carregando as 3 chaves composite. Cobre a wiring proof — uma regressão que rompa qualquer splice (LogMonitor publica shape errado, ZoneTracker dropa stage, RunSnapshotSaver chama `GetCheckpoints()` legacy, etc.) trips aqui mesmo se cada unit test isolado passar.

  **Decisões resolvidas vs as Open Questions originais do BACKLOG.** Q-B1.1 — confirmado empiricamente que cruel não emite `[SCENE]`, único sinal é a linha area-gen com prefixo `C_`. Q-B1.2 — migração das runs antigas: stage ausente vira `"normal"` no Load (transparente, sem migration pass). Q-B1.3 — INI schema: `Act<N>NormalMs` / `Act<N>InterludeMs` no `[checkpoints]` (não section nova). Q-B1.4 — checkpoint key: composite string `"act|stage"` (compatível com IniFile, lexicograficamente ordenável, regex-friendly). Q-B1.5 — stage no detail Map: campo separado `stage`, **não** baked no `note` (preserva o regex `(?:Ato|Act)\s+(\d+)` que dezenas de tests dependem).

  **Gap conhecido (não bloqueante).** `RunHistoryRepository._SerializeDetail` continua 5-field (`category|label|ms|note|timestamp`) — o campo `stage` no detail Map é DROPADO na persistência. Hoje não importa: upstream ainda não emite per-(zone, stage) totals (`ZoneTrackingService` não tem `_totalsByStage`), então toda detail row tem `stage="normal"` por default mesmo. Round-trip via disco trata stage ausente como `"normal"` no Load, então comportamento bate. Quando o pipeline upstream for ligado (futura iteração, possivelmente B6 quando `Route` ganhar dimensão de stage), a serialization vai precisar estender pra 6-field — round-trip test obrigatório nessa hora.

  ToS compliance (GSG §18): zero risco. Mudança puramente de tracking interno; nenhuma nova leitura de game files (a linha area-gen já era parseada pré-B1 — só ganhou um branch de stage), zero simulação de input, zero rede. **No risk** under PoE2 Terms of Use clauses 7(b–f, i). GSG §17 anti-regression review: 24 items revisados, nenhum violado; em particular **item 1 (hydrate-before-subscribers)** preservado (RunStarted{hydrated:true} continua tratado por short-circuit em todos os subscribers — ActCheckpointTracker, ZoneTrackingService, etc.), **item 2 (round-trip de persistência)** preservado com testes round-trip por (act, stage) em PB repo, history repo, e ambos os snapshot saver paths, **item 3 (PB com warning em falha)** preservado — schema novo continua usando o mesmo `WarningSink` path do legacy. Suite cresceu de ~2622 (pré-B1) pra ~2683 testes no fim: +10 LogMonitor, +20 ZoneTracker/ActCheckpoint, +20 PB/history/recorder/average, +13 plot builder, +3 integration. Pre-B1 runs no disco carregam sem erro (chaves `Act<N>Ms` viram normal-only). Pre-B1 PB INIs idem. Plot dialog em pre-B1 history mostra Interlude filter vazio — honest result, esperado.

- **Route authoring blocks duplicates and supports per-zone notes; the overlay surfaces the note for the current zone.** Two coupled changes that close the remaining gaps in the route surface: (a) the editor used to accept duplicate zones silently — a `Route.Add("Mud Burrow")` after "Mud Burrow" was already in the list would happily push a second entry, and the overlay highlight would track one of them while the other accumulated time on its own row — confusing because the speedrunner reading the overlay can't tell which copy is "the current visit"; (b) the editor surfaced zone names only, so a runner with a vendor checkpoint or a "skip optional pack" reminder had no place inside the tracker to write it down. Dedupe became a hard rule (route as set, not list) and per-zone notes ride on the same hardening — a single text field per zone with no granularity below that, since with no duplicates a zone IS its slot in the route.

  **Domain layer (`Route`).** The constructor now silently dedupes its input array (first-wins, case-insensitive via a `seen` Map with `CaseSense="Off"`) AND accepts a second optional argument `notes` — a `Map<zoneName, noteText>` whose keys are lowercased on store so `Route.GetNote("MUD BURROW")` and `Route.GetNote("mud burrow")` resolve to the same entry. New instance methods `HasZone(zoneName)`, `GetNote(zoneName)`, `SetNote(zoneName, text)`, `GetAllNotes()` round out the API; `Add(zoneName)` now returns `false` on duplicate (the silent-no-op contract that prompted the dedupe-up-front guard in the Settings dialog); `Remove(idx)` drops the matching note from the internal map; `Reset()` preserves notes (they're authoring data, not per-run state — a route reset means "haven't started this campaign", not "forget the tips I wrote"). `SetNote(zoneName, "")` (or any whitespace-only value) deletes the entry rather than storing a no-op empty string — mirrors the contract used everywhere else in the codebase where blank input means absent. Notes for zones NOT in `_zones` are kept defensively (the `Route(zones, notes)` ctor doesn't strip orphans) so a hand-edited INI where the user removed a zone but forgot to clean up the `[Notes]` section round-trips without losing data.

  **The `Trim()` whitespace gotcha.** AHK v2's `Trim(str)` default omit-chars is `" \t"` (space + tab) and does **NOT** include `\r` / `\n`. A note that consisted of just `"\t\n"` (tab + LF) sneaked past the `if (Trim(text) = "")` empty-check in the original implementation and ended up stored as an "effectively blank" entry. The fix is explicit: every empty-check on note text passes `" \t\r\n"` as the second arg to `Trim` so all four whitespace chars collapse to `""` uniformly. The same gotcha applies in three places — `Route.__New`'s notes loop, `Route.SetNote`, and `RouteRepository._LoadNotes` — all three were patched for parity.

  **Persistence layer (`RouteRepository`).** The INI gains an optional `[Notes]` section after `[Route]`, with one `<zone>=<encoded text>` per non-empty note. Encoding is symmetric backslash escapes: `\` → `\\` and LF → `\n` (CR is dropped on encode — INI lines can't carry literal CR/LF, and the runtime renders multi-line text via the `\n` escape on decode). The decode pass scans char-by-char and resolves `\\` before `\n` to avoid the trap where `"\\n"` decodes to a literal newline (correct: `\` + `n`, not LF). `Save` omits the section entirely when the route has no notes — the on-disk file stays clean for routes that haven't been annotated. `ExportToFile` / `ImportFromFile` round-trip notes the same way zones do.

  **UI layer (Settings).** The dialog window grew from `WINDOW_W=560` to `WINDOW_W=880` to open a 300 px column on the right of the ROUTE section for a Notes editor. The column carries a header label (`"Notes for: <selected zone>"` or a fallback prompt when nothing is selected) and a `Multi +VScroll +Wrap WantReturn Limit500` Edit bound to the ListBox's selection: a new `OnEvent("Change", ...)` handler on the listbox stashes the Edit's current content into an in-memory `_routeNotes` buffer under the **previous** zone's lowercased key, then populates the Edit with the **newly-selected** zone's note (or empty if it has none). `_OnRouteAdd` rejects duplicates up front with a `SpeedKalandraMsgBox` (`Iconi`) explaining that routes can't carry the same zone twice plus the time-accumulation semantic that makes the constraint sensible (every visit's elapsed folds into the single row's running total via `ZoneTrackingService._FlushActive`, so a "second visit" was never a separate slot to begin with); it also logs the rejection at `INFO` for diagnostics so headless tests have an observable signal where the MsgBox isn't. `_OnRouteRemove` drops the corresponding key from `_routeNotes` AND clears the right-panel binding when the removed zone was the one currently being edited — without that second cleanup, the subsequent stash would resurrect the deletion by writing the Edit's content back under the (now stale) key. `_SaveRouteIfWired` flushes any unstashed in-Edit content via `_StashCurrentNoteFromEdit()` before serializing through `Route(zones, notes)`, so a user who types a note and immediately clicks Save (without first navigating to another zone) doesn't lose the input.

  **Overlay layer (`RouteWidget`).** When the current zone has an authored note, an extra row renders directly below its name on the overlay: background `Theme.Color("surface3")` to differentiate from the zone rows' `"surface"`, text in `"muted"` at a smaller font (`NOTE_FONT_SIZE_BASE=8`, two pt below the zone-row font), full inner width (covers both the name column AND the time column — a free-form text block with nothing to align against on the right). The render loop converted from index-based Y arithmetic (`rowY := padding + (i-1)*rowH`) to a cursor-based `currentY` that grows by `rowH` per row and by `noteHeight` after the current row when a note is rendered. Note height is estimated via `_EstimateNoteLines(note, availW, fontSize)` — splits on `\n` and approximates wraps via an `avgCharW = fontSize * 0.55` ratio against the available width — then multiplied by `NOTE_LINE_HEIGHT_BASE=14` and padded with 4 px so the bottom line doesn't touch the row's edge. The estimate is conservative (overshoots slightly on long lines so the last visible line isn't clipped); a real GDI measurement would be more accurate but isn't worth the complexity for typical sub-200-char tips. The 500 ms live tick that updates per-zone times does NOT touch the note row — notes are static between `RouteChanged` events, so re-rendering them every tick would just churn the Gui without producing new pixels.

  ToS compliance (GSG §18): pure tracker-internal feature — zero game-file reads, zero input simulation, zero network. The note text travels user → Settings Edit → in-memory buffer → INI file → in-memory Route → overlay Text control; no path touches the game. **No risk** under PoE2 Terms of Use clauses 7(b–f, i). GSG §17 anti-regression review: all 24 items reviewed, none violated. In particular **item 1 (hydrate-before-subscribers)** does not apply at the overlay layer because notes are read on-demand at render time via `routeService.GetCurrentRoute().GetNote(zoneName)`, not pre-cached at boot — by the time a render fires, the service has been hydrated. **Item 11 (resize-by-border)** does not apply: `WINDOW_W=880` is a static class constant change, not a user-triggered resize. The `Route.Add` semantic change from "silently allows duplicates" to "silently rejects duplicates" is a behavior change one could argue is anti-regression-sensitive, but it's pinned by two new unit tests (`add_rejects_duplicate_case_insensitive` and the corresponding `RouteRepository::load_dedupes_duplicates_silently_first_wins` for legacy hand-edited INIs that already carry duplicates) and the old test asserting the opposite behavior (`add_allows_duplicates`) was deliberately removed in the same commit.

  67 new/changed tests take the suite from 2454 to **2521**: 18 in `route_tests.ahk` (constructor dedupe — silent first-wins, case-insensitive; notes param accepted, normalized, empty-dropped, type-validated; `HasZone` cases; notes CRUD including delete-on-empty and defensive-copy semantics; `Add` rejecting duplicates; `Remove` dropping notes and no-op when absent; `Reset` preserving notes), 10 in `route_repository_tests.ahk` (Notes section load — absent, present, decode `\n`, decode `\\`, ignore empty; Notes section save — omit-when-empty, present, encode `\n`, encode `\\` first; full round-trip preservation including multiline and backslash-laden notes; export/import round-trip; the previous `load_preserves_duplicates` / `roundtrip_save_load_preserves_duplicates` tests were renamed to `*_dedupes_duplicates_silently_first_wins` / `*_dedupes_silently` to match the new behavior), 11 in `route_widget_tests.ahk` (`_EstimateNoteLines` cases — empty returns 0, short returns 1, hard newlines counted, empty lines counted as a row, long text wraps on narrow column; `_EstimateNoteHeight` cases — zero for empty, exact math for 1-line and 3-line cases; `_GetCurrentNoteFromSlice` cases — no current row, no note, has note, case-insensitive resolution end-to-end through `RouteService.GetCurrentRoute().GetNote`), 20 in `settings_dialog_tests.ahk` (Open hydrates notes from repo with case-insensitive keys; Add rejects duplicates case-insensitive in three casing variations and leaves an INFO log entry citing the offending zone; Remove drops the note for the removed zone and clears `_currentNoteZone` when active; Stash cases — happy path, empty-deletes, whitespace-deletes including bare LF and CR+LF, no-op when zone unbound, lowercase key normalization; Save round-trip and pre-serialize stash; right-panel refresh populating from buffer and clearing on no-selection; full listbox-change integration stashing previous then populating new; static `_ContainsZoneCaseInsensitive` helper). Two real bugs caught by the new suite during initial runs: the `Trim()` whitespace gotcha above (test `constructor_drops_empty_notes` with input `"\t\n"`), and a SettingsDialog smoke test surfaced no test gap but the per-zone note row in the overlay was implemented and verified live before committing.

- **Route refinements: `Default` button to pre-fill the route from the catalog + live per-zone time on the RouteWidget overlay.** Three follow-ups to B4 Commit 3 that close the practical UX gaps the smoke test surfaced: (1) authoring a route from scratch by Add-zone-then-Add-zone-then-Add-zone was tedious, and the user's intent on a fresh install is almost always "give me the whole campaign and let me trim"; (2) the overlay showed the zone sequence but not how long the runner had spent in each, which is exactly the per-segment feedback a speedrunner watches to spot regressions mid-run; (3) the data for (2) already accumulated correctly in `ZoneTrackingService._totals` across re-entries — `_FlushActive` sums every visit into the per-zone total — so the work was making it visible, not making it accumulate.

  **(1) `Default` button in Settings ROUTE.** A new `Default` button sits left of `Import...` / `Export...` (the three buttons reflowed from two `w120` to three `w110` at `x180/x300/x420` to fit the dialog's 528 px usable width, 16 px margin each side off `WINDOW_W=560`). Clicking it replaces `_routeZones` with every non-town zone from the catalog, sorted by `internal_id` parsed numerically. The four-decision pre-coding pass settled on **option D** ("default empty + a button that loads everything from the catalog") over (a) auto-populate on first open (silently overrides what a returning user may have authored in another tool), (b) a curated subset (I'm not a speedrunner; including/excluding side zones is the player's call, not mine), or (c) ship a `default.ini` template (forces the runner to discover and delete what they don't want before they can use the tracker, which is the wrong default).

  **The sort key normalization is the non-obvious part.** Raw lexicographic `StrCompare` on the `internal_id` strings puts `G1_13_1` BEFORE `G1_2` because `'1' < '2'` character-wise — wrong. The fix is `SettingsDialog._InternalIdSortKey(internalId)`, a static helper that strips the `G` / `g` prefix, splits on `_`, and zero-pads each leading numeric run to 4 digits while preserving any letter suffix that may follow (the catalog has `G4_8a` for Arastas and `G4_11_1a` for Ngakanu). So `G1_2` → `"0001_0002"`, `G1_13` → `"0001_0013"`, `G4_8a` → `"0004_0008a"`, `G4_11_1a` → `"0004_0011_0001a"` — lexicographic `StrCompare` on the padded keys now matches the natural campaign order, and the letter suffix sort rule falls out for free (`G4_8` < `G4_8a` < `G4_9`). The helper is static-on-`SettingsDialog` rather than on `ZonesCatalog` because nothing else in the catalog layer needs to sort by `internal_id` today (the catalog already exposes `All()` in insertion order which happens to match the CSV order); promoting this to the catalog would be speculative reuse. A second-helper `_BuildDefaultRouteZones()` builds `{name, sortKey}` pairs and bubble-sorts them via `_SortPairsByKey(pairs)` so the sort doesn't reparse the `internal_id` on every comparison — ~60 elements means O(n²) is invisible, and the in-place mutation reads cleaner than building a parallel index array. The handler `_OnRouteLoadDefault(skipConfirm := false)` asks `SpeedKalandraMsgBox` with `YesNo IconQ` when the buffer is non-empty (so a careless click doesn't lose in-progress edits) and bypasses the prompt entirely when the buffer is empty (no "are you sure" friction for a first-time fill); `skipConfirm` is the test seam — the headless `SpeedKalandraMsgBox` stub returns `"Cancel"` by default, which would otherwise abort every test.

  **(2 + 3) Live per-zone time on the RouteWidget.** `RouteWidget.__New` gains an optional `zoneTracker := ""` 6th argument (`ZoneTrackingService` or empty for back-compat) — type-validated when non-empty. The composition root (`app.ahk` line ~580) was updated to pass `this.zoneTracker` at construction. The render loop now creates **two** Text controls per row instead of one: a left-aligned name control (width = `width - padding*2 - timeColW`) and a right-aligned time control (width = `timeColW = Round(50 * scale)`, style flag `0x2` = `SS_RIGHT`). Both arrays are tracked as `_rowNameControls` / `_rowTimeControls` so a 500 ms `SetTimer` tick can update only the time cells in place without rebuilding the Gui. The current row's time uses the same colour as the name (`Theme.Color("goodStrong")` from the existing `_ColorFor`) and stays bold; upcoming and before rows use `"muted"` so the time reads as a secondary detail next to the name's primary status. Re-entries already accumulate correctly upstream — `ZoneTrackingService._FlushActive(keepActive=false)` adds the visit's elapsed to `_totals[zone]` BEFORE clearing `_activeZone`, so a runner who portals back to a zone they've already visited and leaves again sees the cell jump forward when the next transition lands; the active visit's in-flight time is folded in by `GetZoneTotalWithActive(zoneName)` so the current row's cell ticks up live between renders. The Q4 "no `0:00` noise" decision means zones never visited (or `_totals[zone] = 0`) render blank — the filter lives in `RouteWidget._FormatTime(ms)` (`ms <= 0 → ""`) as the single source of truth, and `_ComputeTimeText` is a thin wrapper that adds the tracker-absent guard plus a defensive `try` around the read. Sub-second positive values still render as `"0:00"` so the time cell appears immediately on zone entry rather than blanking for the first full second; the format crosses from `M:SS` to `H:MM:SS` at exactly the 60-minute mark (rare for a single zone, but the longest runs in the user's history brushed it on Trial of Chaos).

  **The tick lifecycle uses `ObjBindMethod` deliberately.** A naive `() => this._OnTick()` arrow lambda would produce a fresh closure object on every call, and `SetTimer(this._tickFn, 0)` would silently fail to cancel because it'd be looking for the previous reference. The bound method is created once in `__New` (`this._tickFn := ObjBindMethod(this, "_OnTick")`), stored as a field, and reused across the `_StartTick` / `_StopTick` pair — `SetTimer` registers and unregisters it by the same reference. The tick is gated on (a) `this._headless` (tests never want a background timer running, full stop), (b) `IsObject(this._zoneTracker)` (no tracker → nothing to read), and (c) `this._tickActive` (idempotency — re-entrant Show calls don't stack timers). `_StartTick` is invoked from `Show()` AFTER `_Render()` so the first tick has control handles to update; `_StopTick` runs at the top of `Hide()` BEFORE the Gui is destroyed so a stale tick can't fire against a destroyed window. The tick body skips when `_gui = ""`, when `_lastRenderedSlice` is unset, or when `_rowTimeControls` is the wrong type — three defensive gates against a race with a concurrent `_Render` rebuild, though AHK v2's single-threaded message pump makes the race nearly impossible in practice. Each tick reads `GetZoneTotalWithActive` for every row in the slice; calling it for non-current rows is a no-op cost-wise (returns `_totals[zone]` directly) and keeps the loop body uniform.

  **Defensive layout edge.** The render loop clamps `nameW := width - padding*2 - timeColW` to a minimum of 10 px so a pathologically narrow anchor (the tracker's `MIN_WIDTH` is 120 px, but the column-w arithmetic should degrade gracefully if a future anchor is even narrower) doesn't push the name control to a negative width — Windows would throw on the `Gui.Add` otherwise. The time-column reservation `TIME_COL_BASE := 50` is a static class constant scaling with the anchor; 50 px at scale 1.0 holds `MM:SS` (5 chars at the widget's default 10-pt font) plus a small visual margin; `H:MM:SS` (7 chars) clips at the left edge gracefully — the user rarely has a single-zone time above 1h, and when they do the digit count is the diagnostic itself.

  ToS compliance (GSG §18): pure tracker-internal feature — zero game-file reads, zero input simulation, zero network. The tick reads only tracker memory (`zoneTracker._totals` via the public `GetZoneTotalWithActive`), and the Default button reads only the catalog (loaded from `data/zones.csv` at boot). **No risk** under PoE2 Terms of Use clauses 7(b–f, i). GSG §17 anti-regression review: all 24 items reviewed, none violated. In particular **item 1 (hydrate-before-subscribers)** does not apply at this layer: the tick reads tracker state on-demand (every 500 ms), not at boot — by the time the first tick fires (500 ms after Show, which itself is after the composition root finishes wiring), the tracker has been hydrated and `RunStarted{hydrated:true}` has propagated. **Item 11 (resize-by-border)** does not apply: the time column width is deterministically derived from `TIME_COL_BASE × scale`, the same as every other widget dimension.

  15 new tests take the suite from 2439 to **2454**: 6 in `settings_dialog_tests.ahk` covering the Default button (`load_default_replaces_buffer_with_all_non_town_zones_in_internal_id_order` proves the fixture's 4 non-towns land in `G1_1, G1_2, G1_3, G1_4` order rather than alphabetic; `load_default_skips_confirm_when_buffer_empty` proves the empty-buffer fast path bypasses the headless MsgBox stub that would otherwise return `"Cancel"`; `load_default_no_op_when_route_deps_missing` proves the `_HasRouteWiring()` early-return; three sort-key tests — `internal_id_sort_key_pads_numeric_segments`, `internal_id_sort_key_handles_letter_suffix`, `internal_id_sort_orders_G1_2_before_G1_13` — pin both the helper output shape and the motivating bug case where raw `StrCompare("G1_13", "G1_2")` returns negative, the very thing the normalizer reverses); 9 in `route_widget_tests.ahk` covering the zoneTracker dep (constructor accepts the wired tracker, throws on `Map()` as a wrong type, back-compat 5-arg ctor still works; four `_FormatTime` cases covering `M:SS` boundary at 60 s, `H:MM:SS` boundary at 60 min, zero/negative returning `""`, the sub-second flooring to `"0:00"`; three `_ComputeTimeText` cases covering tracker-absent empty, zero-ms zone empty, and positive-totals formatted via the `Hydrate(totals)` API which bypasses the `ZoneChanged → _FlushActive` timing path and isolates the read+format pipeline). The `internal_id_sort_key_handles_letter_suffix` test caught a real bug on first run: the original predicate `ch >= "0" && ch <= "9"` worked for all-digit segments like `G1_1` and `G1_13` but threw `TypeError: Expected a Number but got a String` the moment the loop reached a letter (e.g. "a" in `G4_8a`), because AHK v2's relational operators are numeric and coerce both sides — `"a" >= "0"` is undefined. Fixed by switching to `IsDigit(ch)`, which operates on the string directly without coercion. A subsequent in-game smoke test caught a second bug the suite couldn't reach: the confirm MsgBox option string used `IconQ` instead of `Icon?` (the `Q` shorthand isn't a valid AHK v2 icon code — the valid set is `IconX` / `Icon!` / `Icon?` / `Iconi`); the suite's `load_default_*` tests all pass `skipConfirm=true` to bypass the headless MsgBox stub that returns `"Cancel"`, so the MsgBox option string is never actually parsed in tests. Smoke test surface was the only place this could have been caught — fixed by switching to `Icon?`. A new `_AttachTracker()` helper in the test class disposes the Setup widget, constructs a fresh `ZoneTrackingService` + a tracker-wired RouteWidget, and assigns both to instance fields so Teardown disposes them — a leaked tracker would keep its bus subscriptions live across tests and corrupt subscriber counts.

### Added

- **Settings UI ROUTE tab: in-app authoring closes the "have to hand-edit INI" gap (B4 Commit 3 of 3).** Final piece of B4 Stage 2 — the previous two commits landed the foundation (Commit 1: `Route` / `RouteRepository` / `RouteService` plus `Cmd.ToggleRouteVisibilityRequested`) and the in-overlay surface (Commit 2: `RouteWidget` glued to an anchor timer + toggle arrows on the four timer widgets + the in-game zone re-sync fix for runs that start inside the first route zone). Both shipped functional but **required the user to hand-author `data/routes/<profile>.ini`** — a Settings-dialog-scale piece is what made route editing a tracker feature instead of a tracker plus a text-editor side-channel. This commit closes that gap.

  The new section sits between DISPLAY and HOTKEYS in `SettingsDialog._BuildGui` and renders only when the three route dependencies (`routeRepo`, `routeService`, `zonesCatalog`) are all wired — a single `_HasRouteWiring()` predicate gates both rendering AND the persistence branch in `_PersistAndPublishCfg`, so they can't drift apart. The trio is **ctor-optional** so the legacy 5-arg construction at every existing test site keeps working without refactor: missing any one disables the section but the dialog still constructs cleanly; provided values are type-validated against `RouteRepository` / `RouteService` / `ZonesCatalog`. UX layout (from the four explicit decisions confirmed before coding): a ListBox of the current zones with `▲` / `▼` / `Remove` buttons in an 80 px right-side column for **reorder via buttons** (drag-and-drop on AHK v2 ListBox would require WM_NOTIFY subclassing for a marginal gain over two clicks), an Add row with a DropDownList filtered to non-town zones via `zonesCatalog.All()` + `!z.isTown` + alphabetical sort and a `+ Add` button, a `Visible rows` horizontal **slider** with Range 3-10 / Page 1 / TickInterval 1 driving `cfg.routeRowsVisible` (the slider has a built-in tooltip but it pops over the dialog as a separate window which is disorienting on a settings surface, so a live-updating text label to the right of the track is used instead), and `Import...` / `Export...` buttons — the four-decision list explicitly opted **Import/Export into this MVP** rather than splitting into a Commit 4, since the FileSelect plumbing is small and a complete route surface ships in one commit.

  Editing semantics: `Open()` reads the active profile's route via `_LoadRouteZonesFromRepo()` into an in-memory buffer `_routeZones`. Move/Add/Remove handlers mutate the buffer in place and `_RefreshRouteListBox` rebuilds the displayed rows preserving (or sensibly migrating) the selection — e.g. Remove tries to keep the same position, falling back to the new last row when the deleted item was the tail. All four handlers accept an optional `idxOverride` / `zoneOverride` parameter so headless tests drive them with explicit indexes without a real ListBox; production callers (the OnEvent handlers wired in `_BuildRouteSection`) call without overrides, which makes `_ResolveRouteIdx` read the live ListBox selection. Save persists the buffer to disk through `routeRepo.Save(profileName, Route(_routeZones))` and then publishes via `routeService.Refresh()` so the live `RouteWidget` re-renders against both the new zone list AND the new `cfg.routeRowsVisible` — no dedicated `RouteRowsVisibleChanged` event is added because `Refresh()` already publishes `RouteChanged` unconditionally and the widget reads `cfg.routeRowsVisible` on every render, so the existing subscription path covers both kinds of change for free. Cancel discards every buffer edit by simply closing without persisting; the dialog never touches disk on cancel and `cfg.routeRowsVisible` stays at its pre-Open value.

  Save ordering: settings save runs first (with its existing snapshot/restore rollback semantics, now extended to include `routeRowsVisible`); route save runs second, AFTER the settings TrayTip but BEFORE `this.Close()`. A route save failure does NOT roll back the settings save above — the two repos write different INIs and the user already saw "Settings saved"; a `_SaveRouteIfWired` catch logs a `WARN` ("Route save failed: <ex.Message>", context `SettingsDialog`) and surfaces a focused MsgBox naming the build whose route didn't persist. Route save returning `false` (without a throw, e.g. write permission failure) is also logged as `WARN` (the repo's injected `WarningSink` already surfaced a reason); the route MsgBox is skipped in that case so the user gets one warning surface, not two. **Limitation accepted by design** and documented in the section header docstring: the dialog reads `cfg.profileName` ONCE on `Open()`, so a mid-session Build rename followed by Save persists the route under the NEW name without re-prompting; if the user changes Build but doesn't Save, the editing buffer is still tied to the OLD profile name. Implementing live-reload-on-Build-change would require a confirmation flow that's busier than the edge case warrants — the player normally settles on a Build before authoring routes.

  Snapshot/restore plumbing (`_SnapshotMutableCfg` / `_RestoreCfgFromSnapshot`) gains `routeRowsVisible` so a settings-save failure restores the slider's pre-Save value alongside the other mutable fields — services that hold a reference to `this._cfg` (notably the live `RouteWidget` reading `routeRowsVisible` on every render) never observe a ghost value that didn't land on disk. The two existing tests `snapshot_captures_all_mutable_fields` and `restore_overwrites_in_memory_cfg_fields` were extended with `routeRowsVisible := 7` (snapshot) / `:= 4` then mutated to `9` then restored (restore) assertions to lock that contract.

  Import/Export: Import opens a `FileSelect(1, ..., "Route INI (*.ini)")` dialog, calls `routeRepo.ImportFromFile(path, cfg.profileName)` (full-overwrite into the active profile), reloads the buffer from the freshly imported file via `_LoadRouteZonesFromRepo`, refreshes the ListBox, and calls `routeService.Refresh()` so the live overlay reflects the import without waiting for the user to click Save. Import failure surfaces a MsgBox naming the failing file and the expected schema (`[Route] zones=A|B|C`, UTF-16 LE BOM) without making any assumption about why the import failed — the repo's WarningSink already logged the specific reason. Export first commits the in-memory buffer to disk via `routeRepo.Save` (so the export reflects what the user sees in the ListBox, not the stale disk version a user can be surprised by when they edited but didn't yet click Save), then opens a `FileSelect("S 16", ...)` Save As dialog with overwrite-confirm, then calls `routeRepo.ExportToFile`. Empty-route Export is short-circuited with an "add zones first" MsgBox so the user isn't given a Save As dialog that would silently produce an empty INI. The default export filename uses a private `_SanitizeFileNameStem` helper (mirrors `RouteRepository._SanitizeProfileName` but with `"route"` fallback instead of `"default"` — kept private rather than depending on infra internals to avoid coupling UI to persistence).

  Composition root (`src_v2/app/app.ahk`) was updated to pass the three new ctor args at the existing `SettingsDialog(...)` call site (line 508). The wiring is the only line that changed in `app.ahk`; the route service / repo / catalog are all already constructed earlier in the boot sequence (Commit 1 + 2 territory) and the dialog just receives the existing references.

  A small latent issue from Commit 1 surfaced during the final `git status` check before commit: `RouteRepository`'s `baseDir` was hardcoded to `scriptDir "\data\routes"` instead of accepting an override via `cfgMap`, the way every other path (`iniPath` / `zonesCsvPath` / `logPath` / `runHistoryDir` / `personalBestPath` / `deathLogPath`) does. The integration tests didn't notice because they ran a SpeedKalandraApp without ever exercising a route save — but the moment B4 was fully wired end-to-end, a stray production-format INI (UTF-16 LE BOM, `[Route] zones=...`) landed under `tests_v2/data/routes/Default.ini` on every test run. Fix: `routesDir` was added to `cfgMap` overrides alongside the other six paths, and `SpeedKalandraAppIntegrationTests.Setup` now passes a TempDir for it (cleanup-pool registered). The `.gitignore` also gained a defensive `tests_v2/data/` rule — the underlying fix routes the override through `cfgMap`, but the ignore protects against any future path that gets hardcoded the same way before someone notices.

  A second smoke-test catch immediately after wiring everything end-to-end: with the ROUTE section adding ~250 px (listbox h140 + reorder buttons + add row + slider + import/export row), the dialog's total height pushed past ~1080 px — on a 1920x1080 display Windows placed the window with the title bar (close/minimize) AND the Save/Cancel row at the bottom both outside the viewport, leaving the dialog unusable. Fix has three parts that bundle into a single visual change: (1) the listbox shrinks from `h140` to `h100` and the three side buttons (`▲ / ▼ / Remove`) from `h36` to `h28` with adjusted y offsets so all three still fit alongside the shorter listbox; (2) `_BuildGui` now caps `finalH` at `A_ScreenHeight - 80` so a calculated height past the display never lands on `Gui.Show`; (3) the Gui is constructed with `+Resize` instead of `-MaximizeBox` so the user can stretch the window manually if the cap clipped useful content. The previous `-MaximizeBox` carried a comment justifying the choice on DWM-repaint-cost grounds during overlay drags, but a fixed-size dialog that doesn't fit on screen is a strictly worse trade than a resizable one that does. The `Show` call also gains `Center` so a maxed-out height at least keeps the title bar visible.

  Three case-insensitive identifier collisions were caught during implementation (each documented at the rename so a future refactor doesn't reintroduce the trap): the ctor params are named `routeRepo` / `routeSvc` / `zonesCat` (NOT `routeRepository` / `routeService` / `zonesCatalog`) so the `is RouteRepository` / `is RouteService` / `is ZonesCatalog` checks resolve to the class rather than the local; the `Route` param in `_SaveRouteIfWired` is named `routeObj` (mirrors `RouteRepository.Save`'s convention); the local in `_LoadRouteZonesFromRepo` is named `loaded` (not `route`) for the same reason. **Two of these collisions were actually caught only when the suite ran** — the first draft of the ctor (and the parallel `_MakeDialogWithRoute` test helper) named the params `routeService` / `zonesCatalog`, which compiled cleanly but threw `TypeError: Expected a Class but got a RouteService` at the first `is` check inside `__New` (and `UnsetError: This local variable has not been assigned a value` at the helper's `zonesCatalog := ZonesCatalog(csvPath)` line because the local-in-assignment shadowed the class on the RHS). 72 tests errored on the first suite run; the rename to `routeSvc` / `zonesCat` cleared all of them. CLAUDE.md §3 was updated again with the meta-lesson: **reading the rule before coding does not substitute for the renaming reflex**. The new section adds a concrete heuristic — every time you write `is X` or `X(...)` (a class call) inside a function, look at the function's params and locals BEFORE moving on; if anything matches case-insensitively, rename then, not later. Same family of collision documented now eight times across the B4 feature, with the last two of them after the lesson was already written into CLAUDE.md — a strong signal that the documentation alone is not enough.

  ToS compliance (GSG §18): pure UI surface over the existing route persistence layer — zero game-file reads, zero input simulation, zero network. The only I/O is `routeRepo.Save` / `Load` / `ImportFromFile` / `ExportToFile`, all of which read or write paths the user chose explicitly (the route INI under `data/routes/<profile>.ini` for Save, or a user-picked path for Import/Export). **No risk** under PoE2 Terms of Use clauses 7(b–f, i). GSG §17 anti-regression review: all 24 items reviewed, none violated. In particular, **item 1 (hydrate-before-subscribers)** does not apply at this layer (the dialog doesn't hydrate anything; it reads-on-Open and writes-on-Save), **item 11 (resize-by-border)** does not apply (the `+Resize` flag added to the Settings dialog window in this commit is the standard Windows sizing handle for system chrome, NOT the persisted overlay-resize feature that GSG §17.11 documents being removed — the dialog stores no width/height in INI and reflows nothing proportionally), and the existing PB-fields snapshot/restore contract is preserved — the new `routeRowsVisible` field follows the exact same path as `pbDisplayMode` / `showOutcomeBanner` so it inherits the "no ghost values" guarantee already locked by the persist-and-publish failure tests.

  19 new tests in `settings_dialog_tests.ahk` (the suite goes from 2418 to **2437**): 7 cover the constructor and `_HasRouteWiring` predicate (back-compat with 5-arg call, accepts wired trio, throws `TypeError` on each of the three wrong types, true-when-wired / false-when-any-missing); 9 cover the load + buffer mutation path (Open hydrates from repo with a seeded route, leaves buffer empty when deps are missing, move-up swaps with above, move-up no-op at top, move-down swaps with below, move-down no-op at bottom, remove drops the selected zone, add appends to the buffer, whitespace-only zone names ignored); 3 cover persistence (zones round-trip through `routeRepo.Save` + a fresh `Load`, `routeService.Refresh()` publishes exactly one `Evt.RouteChanged`, route save failure warns log + returns cleanly without propagating); 2 cover helpers (`_ClampRows` enforces the [3,10] bounds inclusive, `_BuildNonTownZoneNames` filters towns from the catalog and returns sorted names). A new `_MakeDialogWithRoute(initialZones, throwingRouteRepo)` fixture handles the setup: tempdir for routes, temp UTF-8 zones.csv with 4 non-towns + 1 town, full trio wired, optional initial seed via `routeRepo.Save`, **and a `routeService.LoadRouteForProfile(cfg.profileName)` priming call** (without which `Refresh()` is a no-op until a profile is loaded, which would have made `save_calls_routeService_Refresh` silently pass for the wrong reason). A new `_ThrowingRouteRepository extends RouteRepository` mirrors the existing `_ThrowingSettingsRepository` for the route-save-failure test.

  CLAUDE.md gains two updates from lessons paid during this commit: §3 (case-insensitive collision) was extended with six concrete examples from the route feature (`route`/`Route`, `routeRepository`/`RouteRepository`, `routeObj`/`Route` param, `svc`/`RouteService` in the widget, `routeSvc`/`RouteService` in the dialog, `zonesCat`/`ZonesCatalog` in the dialog), a new convention guidance section (`Obj` suffix for class-matching params, `svc`/`repo`/`bus` abbreviations for service params, document the rename at the file header when applied so a future refactor doesn't reintroduce the collision), AND a new meta-lesson sub-section noting that the trap was hit TWICE more in Commit 3 AFTER the rule was already written into CLAUDE.md — with a concrete heuristic for catching the collision at write-time rather than waiting for the suite to error out. A new §5 "Adding file in `src_v2/` exige atualizar AMBOS entry points" documents the dual-update pattern (`tests_v2/run_tests.ahk` AND `speedkalandra.ahk`) with the production-log timestamp where the bug fired in Commit 2 (`2026-05-24 03:33:34`), the boot-time symptom ("This local variable has not been assigned a value: <ClassName>"), and a 5-step checklist; the existing §5 ("RunId tem formato canonico") was renumbered to §6 and the anti-pattern list at the bottom gained a matching entry ("Adicionar arquivo em src_v2/ só em tests_v2/run_tests.ahk sem espelhar em speedkalandra.ahk").

- **Route widget surface: anchor-glued walkthrough list + toggle arrows on the four timer widgets (B4 Commit 2 of 3; no Settings UI yet).** Second half of B4 Stage 2 — the route foundation (domain + persistence + reactive service) landed in Commit 1 with no user-visible surface; Commit 2 wires the visible widget and the in-overlay toggle, but **leaves Route editing through a Settings tab to Commit 3**, so today the user has to author `data/routes/<profile>.ini` directly (UTF-8 without BOM works against `IniRead`'s ASCII fallback, UTF-16 LE BOM is the project-wide convention). RouteWidget (`src_v2/ui/route_widget.ahk`, ~330 lines) is standalone — deliberately does NOT extend `WidgetBase` because the route surface isn't user-positionable (its position is derived from an anchor timer widget on every render — persisting an OverlayPosition for it would be a lie since anything the user "moved" would snap back on the next anchor render), doesn't participate in the NORMAL / COMPACT / MICRO mode filter (the anchor does; the route follows whichever anchor the active mode picked), and doesn't need Ctrl-drag, scale-wheel, or the highlight border (same reasoning as `RunOutcomeBannerWidget`: pulling in `WidgetBase` would carry machinery never used). The composition root provides an `anchorResolver` closure invoked on every render — NOT cached — so a `CycleLayout` automatically picks up the new anchor; anchor eligibility (compact.IsModeVisible OR steve.IsModeVisible, never micro) is enforced inside the resolver, the widget only asks "give me the active anchor or an empty string". Glue semantics (sabor 2 from the spec): RouteWidget reads the anchor's live screen geometry via `WinGetPos` at render time — width matches the anchor's width, x matches the anchor's left edge, y sits immediately below (y = anchorY + anchorH); the two windows LOOK like one composite surface but remain independent `Gui` objects so the anchor's machinery (drag, scale, mode filter) stays uncoupled. The new low-level event `Evt.WidgetGeometryChanged` (published by `WidgetBase` post-drag / post-scale / post-position from Commit 1) is the only sync point during user gestures — the route re-renders on drag-end, NOT during the drag motion itself; the sabor 2 trade-off explicitly accepted a brief "detached" frame to keep the drag hot path off the event bus. `Evt.OverlayModeChanged` triggers an anchor re-resolve + re-render (different timer now); `Evt.RouteChanged` rebuilds the rows (zone advanced / retreated / route edited); `Evt.RouteVisibilityToggled` flips Show/Hide. Click-through via `WS_EX_TRANSPARENT` (matches `RunOutcomeBannerWidget` / `WidgetBase` — without it the route surface would steal clicks meant for the game beneath).

  Toggle arrows (`src_v2/ui/route_toggle_arrow.ahk`, ~95 lines) are a shared static helper that emits a single `▾` / `▴` Unicode glyph button. Originally planned for the two Micro widgets, **revised during the visual smoke test to land on the two Compact and the two Steve widgets instead** — Micro is the "minimize real estate" mode by definition, and even one extra glyph muddied the visual contract; Compact and Steve already carry V1/V2/V3 buttons and absorb the arrow without disrupting the layout. Compact Classic + Plus place the arrow at the top-right above the V1/V2/V3 column (custom `x`/`y` overrides on `Build`); Steve Classic + Plus use the default bottom-right corner. `OnClick` publishes `Cmd.ToggleRouteVisibilityRequested` (Commit 1's command); the composition root flips `cfg.routeWidgetVisible`, saves, and publishes `Evt.RouteVisibilityToggled{visible}` for the widget to react to. All four affected widgets gained `_handlerRouteVis` + subscribe-on-construction (guarded by `IsObject(cfg)` for headless tests that pass no cfg), `_OnRouteArrowClick`, `_OnRouteVisibilityToggled`, and unsubscribe in `Dispose` — the bus traffic stays symmetric with every other handler each widget already carries. Settings persistence: new `[Route]` INI section with `WidgetVisible=0|1` (default off so a fresh install doesn't surprise the user with an empty surface) and `RowsVisible=N` (default 5, clamped to [3,10] by `AppSettings._NormalizeRouteRowsVisible`). Both fields are honoured by the widget on the very first render — a user who restores from backup with `WidgetVisible=1` sees the route immediately at Show().

  **Bug discovered + fixed during the visual smoke test (recorded inline because the regression is subtle):** if the player was already physically inside the first route zone when `Evt.RunStarted` fired (autoStart matching dialogue — the user reproduced it with PoE2's "homem ferido" line in The Riverbank, but the same happens with manual New Run after loading into the zone), `RouteService._OnRunLifecycleReset` cleared `currentIdx` to -1 to mark the route fresh — and no subsequent `ZoneEntered` came, because the player hadn't moved. The route highlight disappeared and stayed gone until the next zone transition, sometimes minutes away. Fix: `RouteService.__New` now accepts an optional `zoneProvider := ""` (a closure `() => String` resolving to the player's current zone), and `_OnRunLifecycleReset` calls it after `Reset()` to re-`AdvanceTo` the active zone. The composition root wires it lazily via `() => IsObject(this.zoneTracker) ? this.zoneTracker.GetActiveZone() : ""` because `zoneTracker` is constructed AFTER `routeService` in the constructor, but the closure isn't invoked until the first lifecycle event lands (always post-construction). FIFO subscriber ordering protects the same fix on RunReset / RunCancelled: `RouteService` subscribes BEFORE `ZoneTracker`, so when `RouteService._OnRunLifecycleReset` queries `zoneTracker.GetActiveZone()`, the value is still populated even though `ZoneTracker._OnRunEnded` later clears it. `_OnRunStarted` in `ZoneTrackingService` does NOT clear `_activeZone` either (already established convention, documented in its header), so the same path works for fresh RunStarted. Empty / off-route / throwing providers are swallowed defensively — idx falls back to -1 (legacy behavior preserved).

  Settings UI is **deliberately postponed to Commit 3** because the listbox + ⇈ ⇊ reorder + dropdown filtered by `is_town=0` + slider + Import/Export is a Settings-dialog-scale piece on its own, and Commit 2 already moves enough surface that smoke-testing it in isolation is valuable. Today a power user authoring `data/routes/<profile>.ini` manually gets every feature except editing through a UI; Commit 3 will close the gap.

  ToS compliance (GSG §18): pure tracker-internal feature — zero game-file reads, zero input simulation, zero network; the only I/O is reading/writing `data/routes/<profile>.ini`, same as Commit 1. **No risk** under PoE2 Terms of Use clauses 7(b–f, i). GSG §17 anti-regression review: all 24 items reviewed, none violated. In particular **item 1 (hydrate-before-subscribers)** is pinned by a new integration test — RouteService is constructed BEFORE `runService.Hydrate()` so a hydrated `RunStarted{hydrated:true}` event at boot lands on its subscribed handler; the previous "`_route` left as the empty-string field default → next `ZoneEntered` crashes with `AdvanceTo on String`" production error documented in `data/speedkalandra.log` no longer reproduces. **Item 11 (resize-by-border)** does not apply: the route surface is sized deterministically from `slice.Length × rowH × scale` and the anchor's width, no drag-resize anywhere.

  67 new tests take the suite from 2349 to **2416** — 41 in `route_widget_tests.ahk` (constructor type-checks for bus / cfg / svc / anchorResolver + the same case-insensitive-collision rename pattern as Commit 1 documents at the `svc` parameter; Show / Hide / IsVisible lifecycle; opt-out via `cfg.routeWidgetVisible=false`; anchor resolution including empty-anchor hide; row color mapping per status; the headless render path captures the slice in `_lastRenderedSlice` for inspection; `Evt.RouteChanged` re-renders only when active; `Evt.WidgetGeometryChanged` re-renders only when the changed widget IS our current anchor; `Evt.OverlayModeChanged` triggers anchor re-resolve; defensive paths for malformed payloads on each handler; the `_FakeAnchor` test stub mirrors the real WidgetBase contract `GetHwnd()` / `GetScale()` / `id` so the tests exercise the same render path production does), 6 in `route_toggle_arrow_tests.ahk` (`GlyphFor(visible)` mapping, `Build` returning a Gui control, `RefreshGlyph` updating the glyph in place, `customX`/`customY` overrides honored, defensive on missing onClick), 7 added to `app_settings_tests.ahk` (defaults of both `routeWidgetVisible` and `routeRowsVisible`, the [3,10] clamp on out-of-range values, FromMap parsing, the case where neither key is present), 7 added to `settings_repository_tests.ahk` (round-trip both fields, defaults when the section is absent, clamp re-applied on load so a hand-edited INI with `RowsVisible=15` doesn't crash the widget at boot), 5 added to `route_service_tests.ahk` covering the zone re-sync fix (no-provider preserves legacy `-1` semantics, provider returning a route zone re-syncs to its idx, empty/off-route/throwing providers leave idx at -1), and 2 new integration tests in `speedkalandra_app_integration_tests.ahk` (`route_service_subscribed_before_hydrate_so_it_observes_hydrated_run_started` pins GSG §17.1 by mirroring the existing `hydrated_run_propagates_run_id_to_stats_recorder` pattern; `route_widget_highlight_persists_when_run_starts_inside_first_route_zone` reproduces the in-game bug scenario end-to-end through the real composition root with the actual zoneProvider closure). The `_AssertWired` list in the composition root now includes `routeRepo`, `routeService`, and `routeWidget`, so any future refactor that drops a constructor surfaces at boot with a clear field-name error instead of a cryptic null-method-access during event dispatch.

  Three case-insensitive identifier collisions were caught during this commit (each documented at the rename so a future refactor doesn't reintroduce them): `route`/`Route` would have shadowed the class in `RouteWidget.__New` — fixed by naming the parameter `svc`; `routeRepository`/`RouteRepository` would have shadowed it in `RouteService.__New` — fixed by naming the parameter `repo`; and `routeObj`/`route` would have collided in `RouteRepository.Save` — fixed by `routeObj`. Same trap that already bit `runId`/`RunId` and `route`/`Route` in Commit 1, now documented as a project pattern in the relevant headers.

  Includes wiring: the production entry point `speedkalandra.ahk` was updated alongside `tests_v2/run_tests.ahk` to `#Include` the five new files (`route.ahk` / `route_repository.ahk` / `route_service.ahk` / `route_widget.ahk` / `route_toggle_arrow.ahk`). The dual-update pattern is now standing project rule: **adding a file under `src_v2/` requires updating BOTH entry points; forgetting the production one surfaces as "This local variable has not been assigned a value: <ClassName>" at boot**, which the production log on `2026-05-24 03:33:34` recorded before the second entry was added.

- **Route foundation: domain model + per-profile INI persistence + reactive service (B4 Commit 1 of 2; no user-visible surface yet).** First half of B4 Stage 2 (route walkthrough widget) — the widget itself, the Ctrl+Click toggle arrows on the four timer widgets, the Settings ROUTE tab and the composition-root wiring all land in Commit 2. This commit is plumbing only: `Route` domain model (`src_v2/domain/route.ahk`, ordered `Array<String>` of zone display names with a runtime `_currentIdx` whose semantics are forward-first-then-backward scan in `AdvanceTo` — the runner's path through a route isn't strictly forward, since a trip back to town between Act 1 zones means the next ZoneEntered may be a zone already passed; duplicates allowed in the zone list because the runner may legitimately revisit a zone in a planned route and the nearest-forward rule resolves it); `RouteRepository` (`src_v2/infra/route_repository.ahk`, one INI file per profile at `data/routes/<sanitized_profile>.ini` with schema `[Route] zones=A|B|C` — pipe separator because PoE2 zone names don't contain pipes today, comma was less forward-compatible since names occasionally have commas in other games; encoding UTF-16 LE BOM via AtomicWriter, consistent with PersonalBestRepository and SettingsRepository; profile name sanitization strips Windows-reserved filename characters and collapses empty/whitespace-only names to `"default"`; the `_currentIdx` runtime position is NOT persisted because it's a per-run concept reset on every RunStarted/RunReset/RunCancelled — persisting it would risk showing a stale "you're at zone X" after the runner has moved on between sessions; Import/Export round-trip on the same schema so a speedrunner can share a route with another runner via a single INI file); `RouteService` (`src_v2/app/services/route_service.ahk`, holds the active Route, subscribes to ZoneEntered/RunStarted/RunReset/RunCancelled/ProfileChanged; on ZoneEntered it filters town zones using the `isTown` flag that ZoneTrackingService already enriches into the payload via ZonesCatalog — the service trusts the upstream filter rather than re-querying the catalog, keeping the layer pure; off-route entries are silent no-ops so the runner walking through unlisted zones produces no bus traffic; hydrated runs still Reset() because `_currentIdx` is deliberately not persisted across sessions and the next ZoneEntered will re-sync within seconds; publishes a single `Evt.RouteChanged{currentIdx, totalZones, hasRoute, route}` event when state actually changes so the future widget can render without re-querying the service). A new low-level event `Evt.WidgetGeometryChanged{widgetId, x, y, w, h, scale}` is published by `WidgetBase` at the end of `_UpdatePositionFromGui` (post-drag), `SetScale` and `SetPosition` — implements the "sabor 2" design decision (sync route position to anchor timer on gesture end, not during drag motion) so the upcoming RouteWidget can stay glued below its anchor timer without bus traffic on the drag hot path. The helper `_PublishGeometryChanged` is a silent no-op when the widget isn't rendered (`_gui = ""`), so the existing WidgetBase tests pass unchanged on headless mode. Commands gain `Cmd.ToggleRouteVisibilityRequested` (consumed in Commit 2 by the composition root). ToS compliance (GSG §18): pure tracker-internal feature — zero game-file reads, zero input simulation, zero network; the only I/O is reading/writing `data/routes/<profile>.ini` which is tracker-owned. **No risk** under PoE2 Terms of Use clauses 7(b–f, i). GSG §17 anti-regression review: all 24 items reviewed, none violated; in particular hydrate-before-subscribers (item 1) is preserved by RouteService — the composition root must Subscribe its handlers before calling `LoadRouteForProfile`, which will be pinned by an integration test in Commit 2; resize-by-border (item 11) does not apply since the future RouteWidget has its height calculated deterministically from `cfg.routeRowsVisible × rowHeight × scale` rather than allowing border drag. 134 new tests across three files take the suite from 2215 to **2349**: 51 in `route_tests.ahk` (constructor edge cases including empty/Array/non-Array/empty-string sentinel; queries with the defensive-copy invariant on `GetZones`; Add/Remove/MoveUp/MoveDown with the four sub-cases each of how `_currentIdx` follows the zones it points to through swaps, removes, and inserts; AdvanceTo forward/backward/duplicates/case-insensitive/off-route/empty-input/empty-route; GetVisibleSlice in steady state and at the route boundary and with `n=0` and `currentIdx=-1`), 38 in `route_repository_tests.ahk` (constructor validation including the WarningSink contract; GetPathForProfile sanitization including reserved-char replacement and the empty-name → `"default"` collapse; Load tolerating missing file / missing section / empty key; Load filtering empty pipe entries and preserving duplicates; Save sanitizing pipe/CRLF/INI-metacharacters from zone names with an explicit test that a zone consisting entirely of stripped characters is dropped; full round-trip preserves zone order and duplicates and explicitly verifies that `_currentIdx` does NOT round-trip; Import/Export including the speedrunner-share workflow round-trip), and 45 in `route_service_tests.ahk` (constructor type-checks for bus and repo; LoadRouteForProfile and Refresh state semantics; ZoneEntered advance/town-filter/off-route-silence/retreat with each defensive path for malformed payloads; every Run lifecycle event resets the position including the explicit hydrated-flag test that pins the "don't preserve across sessions" decision; ProfileChanged reloads with defensive paths; Dispose unsubscribes all five subscriptions and is idempotent). Three case-insensitive identifier collision traps were caught and fixed during implementation — `route` shadowing `Route` in the repository, `routeObj`/`loaded` renames in tests, and `routeRepository` shadowing `RouteRepository` in the service constructor — each documented next to the rename so a future refactor doesn't reintroduce the collision.

- **Run-outcome banner (closes the "did it save?" feedback gap).** A transient banner now appears top-centre on the overlay for ~4 s after every Finalize / Cancel / Reset of an active run, announcing what actually happened: `SAVED · 1:12:34 · PB` (green) / `SAVED · 1:12:34` (soft green) / `DNF · 1:12:34` (amber) / `TOO SHORT · 02:42 · NOT SAVED` (orange) / `RESET · NOT SAVED` (grey). The colour comes from `Theme.Color` (`goodStrong` / `good` / `warn` / `accentSoft` / `subtle`), so the palette stays consistent with the rest of the overlay and a future theme tweak picks the banner up for free. The window is click-through (`WS_EX_TRANSPARENT`), `+AlwaysOnTop`, and auto-clears either on the SetTimer fire or immediately on the next `Evt.RunStarted` so it can never sit on top of a fresh run's HUD. Opt-out via the new Settings → DISPLAY checkbox "Show run-outcome banner after each run" (default on so the feedback gap is closed for new users); the flag is live-reloadable through the new `Evt.ShowOutcomeBannerChanged` event — if the user turns it off while a banner is on screen, the widget hides it immediately. Two events orchestrate the flow: `RunSnapshotSaver` now takes an optional `bus` in its constructor and publishes `Evt.RunOutcomeReported{outcome, durationMs, runId, pbChanged}` from each of its three exit paths (saved, dnf, too_short) with a single internal helper that short-circuits on missing bus so existing test setups don't have to wire one. `RunService.ResetRun()` captures `runMs` **before** calling `timer.Reset()` and publishes the same event with `outcome:"reset"` (gated on `wasActive` — a reset on idle state has no user-meaningful outcome to report, only the lifecycle `RunReset` still fires for downstream state-clearers). `RunOutcomeBannerWidget` (`src_v2/ui/run_outcome_banner_widget.ahk`, ~290 lines) is standalone — it deliberately does NOT extend `WidgetBase` because the banner isn't user-positionable, doesn't participate in mode switching (NORMAL / COMPACT / MICRO), and doesn't need scale, Ctrl-drag or the highlight border. The widget is constructed once at boot in `app.ahk`, added to `_AssertWired`, and disposed in `Stop()` so a Reload cycle doesn't leave a stale Gui handle behind. Persistence: new `[Display].ShowOutcomeBanner` INI key, default `"1"` so an upgrade over an older INI keeps the banner on (opt-out semantic preserved); `AppSettings.showOutcomeBanner` field, defaults `true`. ToS compliance (GSG §18): the widget reads NO game files, simulates no input, makes no network call — it's a pure overlay surface driven by tracker-internal events. **No risk** under PoE2 Terms of Use clauses 7(b–f, i). GSG §17 anti-regression review: all 23 items reviewed, none violated; in particular the run-completion save path is untouched (saver still runs in `OnBeforeFinalize` / `OnBeforeCancel`, threshold and PB-eligible-totals logic unchanged), `Stop()` disposes the new widget alongside the other GUI surfaces, and the new event types follow the existing `Evt.*` naming. ~30 new tests across six files: 5 in `run_outcome_banner_widget_tests.ahk` (constructor type-checks, three subscribers wired, message formatter per outcome, colour selection, duration formatter for HH:MM:SS / MM:SS / negative-clamp, cfg.showOutcomeBanner opt-out, RunStarted clears, ShowOutcomeBannerChanged{newValue:false} hides while {newValue:true} stays inert, defensive paths for malformed payloads, Dispose idempotency); 7 in `run_snapshot_saver_tests.ahk` (outcome publishing for the three Save() exit paths, pbChanged round-trip, silent when bus is missing, silent when `runHistory.Save` returns false); 5 in `run_service_tests.ahk` (ResetRun publishes outcome when active, captures pre-reset duration, pbChanged always false, idle reset stays silent, publish order RunReset → RunOutcomeReported); 2 in `app_settings_tests.ahk` (default true, FromMap defaults true on missing key); 2 in `settings_repository_tests.ahk` (round-trip both directions, missing key defaults to true); 4 in `settings_dialog_tests.ahk` (snapshot includes the field, restore replaces it, `ShowOutcomeBannerChanged` fires on flip). **Mudança A do plano original (rename `Cancel` → `SaveAsDnf`) foi deliberadamente pulada** — a investigação mostrou que `Commands.CancelRunRequested` é definido mas nunca publicado em código de produção (sem hotkey, sem botão, sem auto-trigger), apenas testado. Renomear sem entry point user-facing seria over-engineering pelo padrão do GSG §0 ("não trocar simplicidade por aparência de arquitetura"); o rename deve esperar até a ação existir como surface real.

- **PB display mode toggle (`cfg.pbDisplayMode`).** New Settings → DISPLAY checkbox swaps every PB-related surface in the overlay (the displayed value AND the live-timer color comparison target) between the all-time PB and the average of the latest five completed runs. Five widgets touched — Steve Plus, Compact Plus, Compact Classic, Steve Classic, Micro Plus — covering every layout that consults `_pbService` (only Micro Classic is excluded, since it doesn't read PB at all). Visual differentiation in `avg5` mode is per-widget: Steve Plus prefixes the bare value with a tilde (`~ MM:SS`, tilde = approximation), Compact Plus's block sub-labels swap `PB MM:SS` → `AVG MM:SS`, Compact Classic's line2 chip swaps `PB ZZ:ZZ / TT:TT` → `AVG ZZ:ZZ / TT:TT`, and the two color-only widgets (Steve Classic, Micro Plus) follow the new target silently — "current below target" still reads green either way, so the comparison semantic stays consistent across modes. Unlike `layoutVariant` (which requires a restart because widget GUIs are built once at boot), this flag is **hot-reloadable**: `SettingsDialog._OnSave` publishes the new `Evt.PbDisplayModeChanged` event, every PB-using widget subscribes, resets its derived caches (timer color, PB chip text) and calls `_Refresh` — no restart, no widget rebuild. New service `RunAverageService` (`src_v2/app/services/run_average_service.ahk`) does the math: pull-based mirror of `PersonalBestService` over `RunHistoryRepository`, with `GetAverageRunMs` / `GetAverageRunMsForAct(N)` / `GetAverageZoneMs(zone)` mirroring the three PB query surfaces. The window is hard-wired to `N_RECENT := 5` (single edit point for future UI surfacing). Per-act denominator counts only runs that *reached* that act (an Act-1-only run contributes to Act 1's average, not Act 3's); per-zone denominator counts only runs that *visited* the zone, and multiple visits within one run are summed before averaging — same shape as `ZoneTrackingService.GetZoneTotalWithActive`, which is what the PB comparison basis uses. Categories `loading` and `morte` are excluded from zone averages, matching the filter `PersonalBestService.RebuildFromHistory` applies. Two caches with separate lifetimes: run + per-act averages are built from `LoadSummaries(5)` (fast — meta + totals + checkpoints, no details), per-zone averages are built lazily on first `GetAverageZoneMs` after invalidation via `ListRunIds(5)` + `Load(runId)` per run (slow path — 5 INI reads — but bound to N_RECENT and cached until the next dirty flip). Cache invalidation subscribes to `Evt.RunCompleted` AND `Evt.RunCancelled` (both can write a new INI to disk; `RunCancelled` saves when the run lasted at least 3 minutes, so the safe superset re-loads on both) plus a manual `Invalidate()` hook for callers that change runs outside the finalize flow (run-history delete, import). Persistence: new `[Display]` INI section with key `PbMode=pb|avg5`; any value other than the literal `"avg5"` normalizes to `"pb"` in both `AppSettings.FromMap` and `SettingsRepository._LoadDisplay` (same defense-in-depth pattern `layoutVariant` uses). Defensive `_IsAvg5Mode()` helper in each widget performs a *dual check* — cfg present AND avgService injected AND `cfg.pbDisplayMode = "avg5"` — so a widget constructed without the service falls back to PB rather than render stale/empty values. Composition root (`src_v2/app/app.ahk`) constructs `RunAverageService` after `PersonalBestService`, threads it into all five widget construction sites as the final positional arg, and adds `"runAverage"` to `_AssertWired` so a regression that drops the wiring fails at boot. ToS compliance (GSG §18): the new service reads ONLY `data/runs/*.ini` (files this tracker itself wrote), with zero game interaction — no `Client.txt` read, no input simulation, no GGG API or website access — so the feature is **no risk** under PoE2 Terms of Use clauses 7(b–f, i). ~50 new tests across four files: 31 in the new `run_average_service_tests.ahk` (constructor type-checks, empty history, 1/2/5/7-run averages, per-act-by-act-reached, zone visit summation + cross-run averaging, defensive copies, cache invalidation via both events + manual hook, Dispose idempotency); 4 in `app_settings_tests.ahk` (default `"pb"`, accepted `"avg5"`, fallback on invalid, case-variation tolerance); 2 in `settings_repository_tests.ahk` (Display section round-trip, fallback on invalid INI); and 1 in `settings_dialog_tests.ahk` (publishes `PbDisplayModeChanged` with old/new modes on save). Spec lives in `PLUS_LAYOUTS_SPEC.md` §13; the placement reflects the Plus layouts being the largest PB-display surface, but §13.2 documents that the feature applies equally to Classic widgets.

- **Death Stats by Zone aggregate view.** New feature that answers "where do I die most often, across all play sessions" — surfaced via a "Death Stats" button in `RunStatsPlotDialog` (the run-statistics window, the natural home for cross-run aggregations). Three layers wire together at the composition root. (1) `DeathLogRepository` (`src_v2/infra/death_log_repository.ahk`) maintains an append-only `data/deaths.csv` with four columns (`ts`, `zoneName`, `patch`, `profile`). The file is created lazily on the first `Append`, so a fresh install does not carry an empty CSV. `Trim(zoneStr) = ""` rejects empty and whitespace-only zone names silently (a legitimate gap when a death fires before any `ZoneChanged` seeded the active zone); CR/LF in any field is rejected via the injected `WarningSink` (tag `[DeathLog]`) to prevent the same INI/CSV-corruption class of bug already documented for `RunHistoryRepository`. (2) `DeathStatsService` (`src_v2/app/services/death_stats_service.ahk`) is a pure read service: `Aggregate(filter := "")` returns `{totalDeaths, perZone, availablePatches, availableProfiles}` in one pass over the in-memory CSV. No cache — every call re-reads the file, which is fine because the dataset is small (one row per death across all play sessions) and the dialog is opened on demand. Town zones are dropped via `ZonesCatalog.IsTownName` when a catalog is wired (defensive against unknown zones — a zone the catalog doesn't recognize passes through to the chart so a new game patch doesn't silently hide the player's deaths). `availablePatches` / `availableProfiles` are extracted from the **whole** dataset, not the filtered subset, so the dialog's two dropdowns stay stable as the user cycles through filters; `perZone` is sorted by count desc via a stable insertion sort, and the available lists are sorted case-insensitively via `StrCompare(..., 1)`. (3) `DeathStatsDialog` (`src_v2/ui/death_stats_dialog.ahk`) subscribes to `Cmd.OpenDeathStatsRequested` and renders a ListView with three columns (Zone / Count / Bar). The Bar column is an ASCII proportion (`█` U+2588, max 30 chars) because AHK v2's ListView has no per-cell custom-draw hook short of subclassing via `WM_NOTIFY` (fragile and out of scope) — ASCII stays readable in the default font, copy-pastes cleanly into a Discord report, and survives HiDPI scaling without code. The two filter dropdowns use `"(All)"` as the no-filter sentinel (it's never a valid stored value: the upstream handler reads `cfg.gamePatch` / `cfg.profileName` which default to `"Unknown"` / `"Default"` on a fresh install, safe by inspection). Selection is preserved across `_Refresh` rebuilds and falls back to `"(All)"` if the previously-selected value vanished from the dataset.

  Composition-root wiring (`src_v2/app/app.ahk`) uses a **separate** subscriber for `Evt.DeathDetected` from the existing death-penalty handler (`LiveReconfigurationHandlers.ApplyDeathPenaltyToTimer`). The two have different concerns — live-timer adjustment versus append-only history persistence — and splitting them keeps each handler trivial to read and lets a future change to one path not risk the other. The new `_OnDeathDetectedForLog` reads the active zone via `ZoneTrackingService.GetActiveZone()` (canonical resolved name from the catalog, NOT a raw engine id — the §17 anti-regression item is preserved by construction), captures `cfg.gamePatch` and `cfg.profileName`, and calls `Append`. The event `data` parameter is ignored: `LogMonitorService`'s upstream filter already validated `character` against `cfg.characterName`, so by the time this fires we already implicitly own the run that died. The death log is **independent of run lifecycle**: deleting a run via `RunHistoryDialog` removes the `.ini` file and rebuilds PBs but does NOT retract the deaths that run contributed to `deaths.csv`, because the per-zone aggregate is meant to answer "where does the player die across all play sessions, ever" — coupling it to history would make the chart jump around every time the user prunes test aborts. Documented as a design constraint in `KNOWN_ISSUES.md` ("Death log is decoupled from run history").

  Adjacent changes carried in the same feature: the "Profile name" label in `SettingsDialog` is renamed to "Build" (vocabulary shift toward the PoE-player audience — a "build" is what they recognize); the underlying field is still `cfg.profileName`, and `AppSettings` / `SettingsRepository` / `DeathLogRepository` are unchanged — only the visible label moves. `build-dist.ps1` adds `data/deaths.csv` to `$ExcludeFiles` so a release zip never ships personal play data, and `tests_v2/build/test_build_dist.ps1` gains a sentinel row in `$mustFilter` proving the exclusion holds. 61 new tests across four files take the suite from 1874 to 1935: 18 in `death_log_repository_tests.ahk` (header round-trip, whitespace/empty rejection, CR/LF warn paths, malformed-row skip on Load, missing-file empty Load), 22 in `death_stats_service_tests.ahk` (aggregation correctness, filter combinations, town drop, stable sort on count ties, case-insensitive available sort, full-dataset available extraction), 4 in `speedkalandra_app_integration_tests.ahk` (constructor wiring, end-to-end DeathDetected → CSV row with active zone + patch + build, silent skip when no active zone, end-to-end aggregation via the production service), and 17 in `death_stats_dialog_tests.ahk` (constructor type-checks, `Cmd.OpenDeathStatsRequested` subscription, lifecycle idempotency, headless gate keeps `_gui = ""`, and the `FormatBar` static helper across zero/equal-max/half/clamp-above/min-one-block/zero-max-defensive/non-number-defensive cases).

- **Death Stats "All-time (from log)" view.** Same `DeathStatsDialog` gains a second data source: a button at the bottom-left toggles between the live CSV view (above) and a one-shot scan of the raw `Client.txt`. The all-time view is generated synchronously on each click, displayed in the same ListView (so the user reads the same Zone / Count / Bar layout regardless of source), and never persists — no cache survives the toggle to live, and a fresh `Open()` always starts in live. The toggle exists because the CSV-backed view only knows about deaths recorded since the user first installed SpeedKalandra, while many players' `Client.txt` reaches back months or years; the all-time scan surfaces that older history without ever writing it into the app's data directory. Three pieces wire together: (1) `DeathLogScanner` (`src_v2/app/services/death_log_scanner.ahk`, ~220 lines, 26 tests) is a pure standalone reader: receives a `logPath` and an optional `characterFilter`, streams the file via `FileOpen + while !AtEOF + ReadLine` (UTF-8 forced so non-ASCII zone names survive non-English Windows installs), parses two line types with regexes duplicated by design from `LogMonitorService._ExtractScene/Death` (header explains why duplication beats abstraction for two patterns), resolves zone names through `ZonesCatalog` the same way the live tail does, and returns `{success, errorMessage, totalDeaths, perZone, linesScanned, skippedNoZone}`. The `skippedNoZone` counter surfaces in the dialog total label as `(skipped: N outside any known zone)` when non-zero — typical when the log begins mid-session before any `[SCENE]` has been parsed. `_AssertWired` in the composition root now requires the scanner, catching wiring regressions at boot. (2) `DeathStatsDialog` gains `_mode` (`"live"` / `"alltime"`), `_alltimeResult`, `_scanner`, `_cfg` fields and the public hooks `GetMode()` / `IsInAlltimeMode()` / `ToggleAlltimeMode()` (the last exposed so headless tests can drive the toggle without firing the button OnEvent). The toggle reads `cfg.logFile` at call time, not at construction — the user may have changed the path in Settings since opening the dialog, and reading live avoids stale state. The view deliberately passes an **empty character filter** to the scanner instead of `cfg.characterName`: "all-time" is meant to include every character the player ever ran on the install, and current PoE2 builds do not emit `has been slain` for bosses (verified empirically against a real Client.txt that surfaced only 3 deaths when filtered to the active character versus the player's actual history of multiple alts), so dropping the filter does not pollute the result with boss kills. Failed scans (missing file, read error) leave the mode at live and surface a `MsgBox` (in production) or fail silently (in headless); successful scans flip the mode and call `_Refresh`, which disables the Patch/Build dropdowns (they're meaningless on the raw log) and rewrites the header to `"Death Stats - All-time (from Client.txt)"`. (3) An `Export...` button appears in alltime mode only — the live view's data is already at `data/deaths.csv` on disk where the user can copy it with a file manager, so a second export path would be redundant. The export delegates to a static `FormatExportCsv(perZone)` (semicolon-quoted UTF-8 via `CsvFile.FormatRow`, so the output is round-trippable through the same CsvFile parser the dialog otherwise reads) plus an `AtomicWriter.WriteAll` to the user-chosen path. The default folder is `%USERPROFILE%\Downloads` (falls back to `A_MyDocuments` if Downloads is absent — some corporate profiles remove it); the default filename embeds a timestamp so consecutive exports don't overwrite each other. 35 new tests take the suite from 1935 to 1970: 26 in `death_log_scanner_tests.ahk` (constructor type-check, path/file validation, basic parsing, character filter behaviour for both empty and non-empty, catalog effects including town drop and unknown-zone passthrough, no-zone edge case, sort + stability, the two static parsers); 9 in `death_stats_dialog_tests.ahk` (constructor type-checks for scanner + cfg, the mode-toggle lifecycle, an end-to-end assertion that the dialog ignores `cfg.characterName` and counts every death in the log, and the six `FormatExportCsv` cases including empty array, non-array defensive, single/multiple rows, quote escaping, internal-quote doubling); 1 added to the existing `speedkalandra_app_integration_tests.ahk::constructor_creates_death_log_components` to assert `deathLogScanner is DeathLogScanner` (the type check at the composition root mirrors the existing assertions for `deathLog` and `deathStatsService`).

- **Death Stats all-time view: cruel difficulty detection + campaign-only filter.** Empirical investigation of a real 115 MB Client.txt revealed two issues with the initial all-time view design: (1) the view was inflating counts with deaths in hideouts / atlas maps / endgame trials — noise that buries the campaign signal a speedrunner cares about, and (2) cruel-difficulty zones (the Acts 1–3 replay before endgame) were being **silently lost** because PoE2 does NOT emit `[SCENE] Set Source` for cruel zones — only `Generating level X area "<code>"`, with a `C_` prefix on the code to mark cruel. The view's only zone-detection path was the SCENE line, so cruel deaths were either attributed to the last normal zone seen before crossing into cruel (silent mis-attribution) or dropped as "before any zone" (silent loss). Both behaviours were verified against the real log — 25 `Generating level area "C_*"` events appeared with zero corresponding SCENE Set Source lines in the following 100 lines of each.

  Fix: `DeathLogScanner` gains a **third parser**, `_ParseAreaGen`, that extracts the area code from `Generating level X area "<code>" with seed N` lines and a new resolution path `_ResolveAreaCode(code)` that handles the `C_` prefix by stripping it, looking up the underlying internal id in the catalog, and appending `" (Cruel)"` to the display name when cruel was detected. The catalog itself does NOT gain cruel entries — the suffix is dynamic and works for every campaign zone (~77) without doubling the CSV. Scanner state machine now has three signals in precedence order: area-gen line (highest, the only cruel-aware signal), SCENE Set Source (fallback for normal-difficulty and when the area gen is truncated), and `has been slain.` (counted against the most recent resolved zone, or `skippedNonCampaign` if none). A SCENE that resolves to `""` (hideout, endgame, town, unknown) now resets `currentZone` so a death after leaving a campaign zone doesn't get mis-attributed to the campaign zone the player just left.

  Campaign-only filter: anything not in `data/zones.csv` is dropped at resolution time and the count surfaces in `skippedNonCampaign`, displayed in the dialog header as `(skipped: N outside campaign zones)`. This covers hideouts (e.g. `Canal Hideout`, code `HideoutCanal`), atlas maps (`Spider Woods`, `Sandspit`, codes `MapSeepage`/`MapAugury_NoBoss`), endgame trials (`The Burning Monolith`, code `P1_5`), and towns (`Clearfell Encampment`, including its cruel variant `C_G1_town`). The drop is transparent — the user sees both `Total deaths: N` and `(skipped: M outside campaign zones)`, so a low total never looks like a silent bug.

  Result-Map breaking change: the field `skippedNoZone` is renamed to `skippedNonCampaign` to reflect the broader semantics (it now counts every drop reason, not just "death before any [SCENE]"). `DeathStatsDialog._RefreshAlltime` and the scanner tests are updated accordingly. The static `_ParseScene` parser also gains an `Interlude` filter — PoE2 emits `[SCENE] Set Source [Interlude]` as a cinematic title card between cruel transitions, alongside `Act N` markers; both are now filtered as non-zone markers. Eight existing tests in `death_log_scanner_tests.ahk` were reworked (renamed for the new contract, assertions inverted where the campaign-only policy changed behaviour) and **eight new tests** were added: `scan_area_gen_resolves_normal_zone_via_internal_id`, `scan_area_gen_takes_precedence_over_following_scene`, `scan_detects_cruel_via_c_prefix_and_appends_suffix`, `scan_cruel_and_normal_same_zone_counted_independently`, `scan_drops_cruel_town_via_c_prefix_on_town_id`, `scan_drops_hideout_area_code_increments_skipped`, `scan_drops_endgame_map_area_code_increments_skipped`, `scan_hideout_after_campaign_resets_currentzone`, plus three new static-parser tests (`static_parse_area_gen_extracts_normal_code`, `_extracts_cruel_code_with_prefix`, `_returns_empty_for_non_matching`) and the `Interlude` filter on `static_parse_scene_returns_empty_for_null_unknown_act_marker_interlude`. The dialog tests are unchanged — all their fixtures use Mud Burrow / The Riverbank which are campaign zones, so the new policy is a no-op for them. Suite goes from 1970 to **1980** tests.

  Known limitation carried over: the **live tracking path** (`LogMonitorService`) still relies only on `[SCENE] Set Source` and therefore has the same cruel blind spot. A run that spans cruel difficulty will record deaths in `data/deaths.csv` attributed to the last normal zone before crossing the threshold. The all-time view, which now uses the better signal, is the recommended way to inspect cruel deaths until the live tail is extended (out of scope for this change).

- **Operational caps on the import surface (gold-standard hardening).** `RunImportService` now refuses files above `MAX_FILE_BYTES = 10 MB` before any `FileRead`, using `FileGetSize` so a 100 MB hand-crafted JSON never consumes 100 MB+ of RAM just to fail later on schema parse. `RunExportFormat` gains six caps enforced inside `ValidateSchema`, with reasoning documented next to each constant: `MAX_RUNS_PER_FILE=5000` (a multi-league archive fits; a million-entry array does not), `MAX_STRING_LEN=500` (zone names cap at ~40 chars in the live catalog; profile/patch are user-defined; 500 leaves headroom for human input without admitting 10 MB of single-field payload), `MAX_DETAILS_PER_RUN=1000` (a 4-hour run at one zone change per 15 s = 960 details, plus per-loading rows), `MAX_TOTALS_PER_RUN=200` and `MAX_ZONE_PBS=200` (77 zones today; 200 absorbs future expansion), `MAX_ACT_CHECKPOINTS=20` (6 acts today; 20 absorbs future expansion). The runId path tightens too: `_ValidateRun` now runs `RunId.IsValid` on the run's `runId` field and `_ValidatePbs` runs it on `runPbRunId`, both producing a clear "has invalid format ('value'); expected YYYYMMDD_HHMMSS with optional alphanumeric suffix" error. Empty `runPbRunId` is explicitly accepted as the well-formed "no PB yet" state. The length cap runs alongside the existing INI-breaking-char check on every textual field at the run, totals, details, and PB layers — a 50 MB profile string would otherwise pass `_FindIniBreakingChar` (no `\r\n[]`) and only fail much later on disk write. 15 new tests in `run_export_format_tests.ahk` exercise each cap (oversize array, oversize string per field, oversize map count, invalid runId format on both run and PB, empty PB runId positive control, legacy `_imported` suffix positive control). 4 new tests in a new `run_import_service_tests.ahk` cover the size gate and pre-read guards (empty path, missing file).

- **Defensive smoke tests for the UI under realistic worst cases.** `CompactLayoutWidget` gains two tests: `estimate_text_w_handles_200_char_zone_name_linearly` proves the `chars × fontSize × 0.6` estimator returns a sane, positive value at 200 characters (the realistic worst-case zone name from a hand-edited INI or a future game expansion with verbose location strings), and `on_zone_entered_accepts_200_char_zone_name_without_throwing` proves the bus handler stores the value verbatim — the rendering side (font shrink, clipping) is delegated to the lazy `_Refresh` tick which is guarded by `if !this._gui return` and can't be exercised headless. `RunStatsPlotBuilder` gains `build_aggregates_100_plus_zone_entries_without_overflow` (120 zones × 60 s = 7,200,000 ms aggregate) and `build_aggregates_100_plus_loading_events_without_overflow` (150 transitions × 5 s = 750,000 ms aggregate) — proves the in-memory snapshot path (which has no `MAX_DETAILS_PER_RUN` cap because it's not gated by the import schema) sums correctly without overflow and produces exactly one detail row per input entry.

### Changed

- **Overlay drag rewritten as event-driven (`WM_MOUSEMOVE` + `WM_LBUTTONUP`) with explicit `SetCapture`; Settings dialog frame slimmed.** The previous drag was a `SetTimer 16ms` polling loop calling `MouseGetPos` + `WinMove` on every tick. Under realistic load — five other `SetTimer` callbacks active on the AHK message pump (log monitor, focus auto-pause, tick emitter, loading detection, Ctrl poll) plus DWM compositing two AlwaysOnTop layered windows when the Settings dialog was open — the drag tick was preempted enough to produce visible stutter (effective ~30 fps with jitter instead of the 60 fps the period requested). The new path installs `OnMessage(WM_MOUSEMOVE)` inside `_OnLButtonDown` and removes it inside `_EndDrag`, so the dragged window moves only when the mouse actually moves (Windows already skips idle moves and a `_lastMouseX/Y` guard catches the rare coalesced replay). `OnMessage(WM_LBUTTONUP)` finishes the drag normally; a 100 ms `SetTimer` watchdog covers the lost-LBUTTONUP edge case (cross-process focus stolen mid-drag — capture is released by Windows when another process takes foreground, and the up event then goes nowhere). The watchdog also serves the `Stop()`/`UnregisterHwnd` cancellation paths via the shared idempotent `_EndDrag` + `_CleanupDrag` helpers, which release mouse capture and tear down the OnMessage handlers; cancellation paths skip `onDragEnd` while normal completion fires it, preserving the prior semantics for widget position persistence. `SetCapture(hwnd)` is called explicitly on LBUTTONDOWN because `_OnLButtonDown` returns 0 to suppress the click on child buttons (V1/V2/V3) — that suppression also blocks `DefWindowProc`, which is where Windows normally auto-captures the mouse on button-down; without explicit capture, `WM_MOUSEMOVE` stops arriving the moment the cursor leaves the overlay window, freezing the drag whenever the user moves faster than `WinMove` can keep up. `Start()` also force-sets `CoordMode("Mouse", "Screen")` so the drag handlers' `MouseGetPos` is in the same referential as the `WinGetPos`/`WinMove` calls (default `Client` would have silently produced wrong deltas when the active window changed mid-drag, and the same fix removes a latent bug in `_UpdateHoverState` which already compared screen-space `WinGetPos` against client-space `MouseGetPos`). The Settings dialog itself lost its `+Resize` flag in the same pass — the window has fixed dimensions (`WINDOW_W=560, WINDOW_H=620`) and never used sizing borders, but the resize-capable DWM frame is more expensive to repaint when an AlwaysOnTop layered window (overlay) crosses it during drag. The previous `static_drag_tick_ms_is_16` regression test is renamed to `static_drag_watchdog_ms_is_100` and the constant `DRAG_TICK_MS` is renamed to `DRAG_WATCHDOG_MS`; the interactive drag itself remains outside the headless test surface (real `OnMessage`/Win32 required), matching the `KNOWN_ISSUES.md` entry that already lists the drag/wheel paths as uncovered. The two changes (drag rewrite + Settings frame) are bundled because the Settings-open case was the trigger that exposed the polling cost; either alone would have helped less.

- **`GAME_EXECUTABLES` consolidated into `GameProcesses.POE2_EXECUTABLES`.** `FocusAutoPauseService` and `LoadingDetectionService` previously each declared the same hardcoded list of PoE2 executable names with a "keep in sync" warning in both files. The list now lives in `src_v2/domain/game_processes.ahk` and both services consume it directly. 6 new tests in `game_processes_tests.ahk` lock the contract (non-empty, every entry `.exe`, no duplicates, canonical standalone and Steam builds present).

- **`build-dist.ps1` release gate now genuinely waits for the AHK suite.** The previous form `& $ahkExe "tests_v2\run_tests.ahk"` was a silently broken gate when invoked from PowerShell 5.1 (which is what the `build-dist.bat` wrapper uses by default). AutoHotkey64.exe is Windows-subsystem, not console-subsystem, and PS 5.1 does not wait implicitly on GUI EXEs called via `&` — it is fire-and-forget. Empirical evidence: the build returned and produced the zip while the AHK process was still running the 50-second suite; the test summary printed *after* the zip was on disk. A null-defensive `$exitCode = $null ? 0 : raw` line, originally added because the CI runner sometimes returned `$null` from native processes, made the bug invisible by mapping the no-wait `$LASTEXITCODE = $null` to a treated-as-success 0. The gate is now `Start-Process -FilePath $ahkExe -Wait -PassThru -WindowStyle Hidden` plus `$testProc.ExitCode` (guaranteed populated by .NET's `Process.ExitCode` after `WaitForExit`, no null-defensive fallback needed); `-NoNewWindow` is deliberately omitted because it caused the original 19-minute hang on the GitHub Actions runner (inherited stdio handles leaving `WaitForExit` blocked on a pipe that never closes). A second layer of defense compares `tests_v2/tests_output.log`'s LastWriteTime against `$gateStartTime` captured before AHK launched — if AHK reports exit 0 but the log is stale, the build aborts loud ("the runner did not produce a fresh log during this build"). The CI workflow (`shell: pwsh`, PowerShell 7) keeps the `&` form because PS 7 *does* wait on GUI-subsystem EXEs invoked via `&`; the local-only nature of the bug is documented in the new comment block above the gate.

- **Test-suite count refreshed in `tests_v2/README.md` and `src_v2/README.md` ("over 1800 tests" instead of "over 1500").** The coverage table also gains `RunImportService (size gate)` under stateful services.

### Removed

- **Steve Plus widget: LINE2 PB surface collapsed to a single bare value.** The Steve Plus chip row originally shipped with *two* PB surfaces — a left-aligned `PB MM:SS` chip next to the XP indicator, and a right-aligned `RUN · PB MM:SS` sublabel — both reading from the same per-act PB source (`pbService.GetRunPbForAct(currentAct)`) because the "overall run PB" the sublabel implied was never actually wired. Players read `"PB 3:33" + "RUN · PB 3:33"` for an entire run, the same number twice. The fix collapses both into one right-aligned, teal-coloured, **unlabelled** `MM:SS` value: the chip is gone, the `RUN ·` framing is gone, the `PB` prefix is gone. The teal color against the muted death/XP chips on the left already communicates "this is a PB" without needing a textual prefix; a labelled chip was either redundant against the colour (just `PB`) or actively misleading (`RUN · PB` on a per-act value). Rendering now reads `× 0   XP                                            3:33` on LINE2, with the bare value rolling forward as the runner advances through acts (PB at end of Act 1, then end of Act 2, and so on). The source stays the per-act PB — the literal overall-run PB exists in `PersonalBestService.GetRunPbMs()` (populated by `UpdateFromRun` on `FinalizeRun`) and is a one-line swap in `_GetRunPbMs` if a future iteration wants it instead, but during an active run the per-act value is what changes mid-run and matches the source the LINE1 timer compares against for its goodStrong/danger color. Internally: the control was renamed `line2_run_pb` → `line2_pb`, the refresh method `_RefreshRunPb` → `_RefreshPb`, the cache field `_lastRunPbText` → `_lastPbText`, the static constant `RUN_PB_W` → `PB_W` (shrunk from 130 px to 64 px because the bare "MM:SS" no longer needs room for the `RUN · PB` prefix), and `FONT_RUN_PB` → `FONT_PB` (size unchanged at 9 pt). The old `line2_act_pb` chip and its `_RefreshActPb` are gone. PLUS_LAYOUTS_SPEC.md §4.2 is rewritten with a four-step "label-iteration history" sub-section explaining the path (chip + sublabel → chip only → sublabel only → bare value) so a future contributor sees the dead-ends and doesn't relitigate them. No tests touched the removed controls or the renamed ones (the existing `steve_layout_plus_widget_tests.ahk` only exercises static helpers + constants by design — live render goes through Win32 and is covered in integration), so the suite is unchanged.

- **Resize-by-border interaction (Plus layouts) removed; scroll is the only resize mechanism again.** A short-lived feature added to the Plus layouts (PLUS_LAYOUTS_SPEC.md §7–§9) let the user drag the right or bottom edge of a Plus widget with Ctrl held to expand or contract the container, with the resulting width/height persisted to `[Overlay].<widgetId>.width` / `.height`. Empirical use during the Compact Plus visual debug session surfaced three problems: (1) the border hit-test threshold was 6 px regardless of widget size, so Ctrl+drag near the V1/V2/V3 button column on the right edge of the Compact Plus widget kept triggering a resize when the user intended a move; (2) the spec called for proportional reflow of inner-block coordinates as percentages of the current widget size, but the actual implementation used static pixel offsets, so a resized widget kept its content positioned for the FIXED dimensions and either truncated or left empty stripes around the blocks; and (3) the gesture was redundant with the existing Ctrl+wheel scaling for every realistic use case — the user can shrink or grow the whole widget via wheel without picking up a non-default aspect ratio. The feature is removed in full rather than fixed: the `OverlayResizeGeometry` helper is deleted, the resize branches in `OverlayInteractionService` (`_dragKind` state machine, border hit-test in `_OnLButtonDown`, resize arm of `_OnDragMove`, `_FireOnResizeBorderEnd`) and the corresponding `RegisterHwnd` signature extension (`onResizeBorder`, `minW`, `minH` parameters) are removed, the `_OnBorderResize` method on each of the three Plus widgets is removed, the `width` / `height` fields on `OverlayPosition` (and their INI round-trip in `SettingsRepository._{Load,Save}Overlay`) are removed, and `LayoutWidgetBase.Show` goes back to `Round(sz["w"] * scale)` for the container size with no width/height layer on top. 32 tests removed across `OverlayResizeGeometryTests` (entire suite, ~24 tests), `OverlayLayoutTests` (8 width/height-specific cases), `SettingsRepositoryTests` (2 round-trip cases), and `OverlayInteractionServiceTests` (the `register_hwnd_*_border*`, `_FindWidget`, `unregister_hwnd_during_resize_*`, `initial_drag_kind_*`, and `stop_resets_drag_kind` cases). Existing INI keys (`<widgetId>.width=0` / `.height=0`) in a user's `speedkalandra.ini` are now ignored — the loader doesn't read them and the saver doesn't write them, so they become inert orphans that a user can delete manually if they wish. Added to GSG §17 anti-regression list so the feature can't drift back in.

- **Debug helpers `SpeedKalandraRunExportSelfTest()` and `SpeedKalandraRunImportDebug()` removed from `speedkalandra.ahk`.** Both were interactive `MsgBox`-based helpers from early iteration of the export/import feature, kept as global functions for ad-hoc invocation via the AHK console after the tray menu items were removed. The regression coverage they were preserved for is now exercised end-to-end by the automated suite (`run_export_format_tests.ahk`, `run_history_repository_tests.ahk`, `run_import_service_tests.ahk`), which is faster, repeatable, headless, and runs in CI on every commit. ~95 lines of dead code dropped from the entry point.

### Documentation

- **`ARCHITECTURE.md` §6 documents the terminal lifecycle and the Dispose-vs-Stop policy explicitly.** Adds a paragraph stating that `Stop()` is terminal (`Start` after `Stop` throws, `Stop` itself is idempotent, reload is a new process via the tray menu's Reload item, the `OnExit` handler in `speedkalandra.ahk` sends defensive modifier key-ups before invoking `Stop`), and a paragraph documenting the audit conclusion that `Stop()` already releases every service with an external resource (AHK SetTimers, AHK `Hotkey()` registrations, file-tail state, the bus interceptor, the three widget Gui handles) — services that only subscribe to the bus expose `Dispose()` for symmetry but are not invoked from `Stop()`, because the bus itself is dropped when the process exits or a new instance is constructed. Calling those Dispose()s would be ceremony with no observable effect.

- **`ARCHITECTURE.md` §5 lists the import caps inline on the `RunImportService` row.** A reviewer reading the services table now sees the same numbers that live in `RunExportFormat` (`MAX_RUNS_PER_FILE=5000`, `MAX_STRING_LEN=500`, `MAX_DETAILS_PER_RUN=1000`, `MAX_TOTALS_PER_RUN=200`, `MAX_ZONE_PBS=200`, `MAX_ACT_CHECKPOINTS=20`) plus the file-level cap (`MAX_FILE_BYTES=10 MB`) and the explicit mention that `RunId.IsValid` is enforced on both `runId` and `runPbRunId`.

- **`ARCHITECTURE.md` §8 corrects the Undo/PB behaviour.** The previous text claimed "Personal bests are not reverted" after Undo, but the implementation has rebuilt PBs from the remaining history via `PersonalBestService.RebuildFromHistory` since Fase 7 of the prior roadmap. The text now describes the actual behaviour (file deleted + PBs rebuilt + tray item auto-expires after 60 s).

- **`SECURITY.md` Anti-cheat / TOS bullet replaces the absolute "does not inject or send inputs" with the actual behaviour.** The app does emit defensive `{Ctrl up}{Alt up}{Shift up}{LWin up}{RWin up}` at process exit and `{Ctrl up}{Alt up}{Shift up}` before opening dialogs from a hotkey, to avoid the AHK "stuck modifier" failure mode. Those key-ups are not gameplay commands. The disclaimer now mentions them explicitly.

- **`src_v2/README.md` fixes the `run_state_repository.ahk` path comment.** The map showed `data/run_state.ini + zone_totals.txt` but the actual paths have been `speedkalandra.ini [RunState] + speedkalandra_zones.txt` since the split-file persistence change.

- **`app.ahk` event-tracing comment corrected.** The previous comment claimed the interceptor was registered "BEFORE any service subscribes" but `EventTraceLogger.__New` only constructs the object; `AddInterceptor` runs in `tracer.Start()` called from `SpeedKalandraApp.Start()` and only when `cfg.eventTracingEnabled`. The comment now states that explicitly and notes that the `RunStarted{hydrated:true}` event emitted during `runService.Hydrate` at the end of `__New` is NOT captured by the tracer.

- **`CONTRIBUTING.md` aligns its "Read-only" and "What I won't merge" bullets with the same key-up nuance.** The merge policy now says "sends gameplay commands" instead of "sends inputs" so the existing defensive key-up doesn't read as a self-violation.

### Added

- **`.gitattributes` fixing line endings and binary handling.** `*.ahk`/`*.ps1`/`*.bat`/`*.cmd` pinned to CRLF (Windows-native tooling — the AHK error dialog and Notepad++ expect it; the build/tray flows only run on Windows); `*.md`/`*.csv`/`*.yml`/`*.yaml`/`*.json` pinned to LF (the cross-platform default, also what GitHub renders cleanest); `*.ini` and images/archives marked binary so git never tries to text-normalize a UTF-16 LE BOM file that `IniWrite` produced (no INI is committed today, but the rule is a guardrail for any future template/fixture). Existing files were intentionally **not** mass-renormalized (`git add --renormalize .` was not run) — only new commits respect the new rules. A separate PR can renormalize the whole repo at once if desired; the diff would be massive and worth handling on its own, not bundled here.

### Fixed

- **Hotkey-interrupted zone visit polluted PB totals (artificial sub-second zone PBs).** Pressing `Ctrl+Alt+F` to finalize a run shortly after entering a new zone left that zone with an artificially low PB candidate equal to the few seconds the player spent there before the hotkey. The same zone, in a normal run that crossed the transition out of it, would land 60–120 s — the interrupted-visit number replaced it because `RunSnapshotSaver` was passing the factual `zoneTotals` straight to `PersonalBestService.UpdateFromRun`, and the factual total at that point included the just-flushed elapsed of the interrupted visit. The fix tracks per-visit elapsed (`ZoneTrackingService._currentVisitMs`, accumulating in `_FlushActive` and resetting on every transition / lifecycle end), reads it from `RunSnapshotSaver.Save` via the new `GetCurrentVisitMs()` query in the `OnBeforeFinalize` hook (after `TimerStopped` flushes the visit but before `RunCompleted` publishes), subtracts it from a copy of the zone totals (`pbZoneTotals`), and passes that copy to `UpdateFromRun`. The factual `zoneTotals` is unchanged — the run's history file and post-run plot still show the full visit time as a historical fact; only the PB candidates exclude it. Permissive case preserved: if the zone had a prior complete visit in the same run (closed via transition) plus the interrupted one, only the interrupted visit's time is subtracted, the prior closed visit still counts as a PB candidate. Single-visit interrupted zones drop out of PB-eligible totals entirely (negative remainder → zone removed instead of recorded with a negative or zero PB). The `(interruptedZoneName, interruptedZoneVisitMs)` pair is persisted into the run's `[meta]` section by `RunHistoryRepository`, so `PersonalBestService.RebuildFromHistory` (the path that runs after `UndoLastSave` and after `Delete` from `RunHistoryDialog`) mirrors the same discount — Undo lands on the exact same PB as the original save, never on the inflated pre-fix one. Legacy runs persisted before this field existed load with the defaults `""` / `0` and skip the discount cleanly (no regression on previously-saved history). The visit accumulator is in-memory only by design (see `KNOWN_ISSUES.md` for the narrow crash + same-zone + hotkey edge case). 37 new tests: 14 on `ZoneTrackingService` (per-visit accumulation, pause/resume preservation, reset on every lifecycle path, finalize-sequence end-to-end), 8 on `RunSnapshotSaver` (discount applied, zone dropped when visit equals or exceeds total, keys injected into buildResult, cancelled runs don't call `UpdateFromRun`), 6 on `RunHistoryRepository` (INI round-trip, legacy default, defensive rejection of `\n` / `[` in `interruptedZoneName`), 7 on `PersonalBestService.RebuildFromHistory` (mirrored discount, drop-on-equal, defensive on exceed, no-op on empty/zero keys, only matching zone discounted, back-compat with legacy runs), and 2 integration tests in `speedkalandra_app_integration_tests.ahk` (original bug reproduction + permissive case) wiring the three layers through the real composition root.

- **Zone internal IDs leaking as human names (engine `G1_2` shown/saved instead of `Clearfell`).** PoE2's `Client.txt` emits `[SCENE] Set Source [<raw>]` with `<raw>` being either a human zone name ("Mud Burrow") or the engine's internal id ("G1_3"). The old `LogMonitorService` copied the raw text straight into `Events.ZoneChanged{zoneName: raw, sceneId: raw}`, so when the engine emitted the id, every downstream consumer keyed its data by the id: `ZoneTrackingService._totals["G1_3"]`, `RunStatsPlotBuilder._AddZoneDetails` finding `FindByName("G1_3")` empty (fell back to category="mapa", act=0, label="G1_3"), `RunHistoryRepository.Save` writing `[totals] G1_3=ms` to `data/runs/{runId}.ini`, and `PersonalBestService._zonePbs["G1_3"]` in `personal_bests.ini`. `GetActTotals()` silently dropped the zone from per-act tallies because `FindByName` of the id failed. The fix resolves the raw text to the canonical human name from `ZonesCatalog` at the single publisher (`LogMonitorService._ResolveZoneToHumanName`): try `FindByName` first (catches human-name lines, recovers canonical case), then `FindById` (catches engine-id lines, returns the catalog's display name), then pass through the raw text for unknown zones (a new game patch or randomized instance still surfaces in the overlay/history — just without act/isTown metadata). `Events.ZoneChanged.sceneId` continues to carry the raw text for diagnostics. The catalog is an optional constructor parameter on `LogMonitorService`; tests/headless setups without one preserve the pre-fix behaviour. The legacy `"You have entered <zone>"` parse branch is also routed through the resolver — not observed in current PoE2 `Client.txt` (every real-world `ZoneChanged` fires from `[SCENE]` today), retained defensively in case the engine reintroduces the line. 8 new tests across `log_monitor_service_tests.ahk` (resolution by id, resolution by name with case recovery, fallback for unknown zones, no-catalog passthrough, constructor type-check) plus 1 end-to-end guardrail in `speedkalandra_app_integration_tests.ahk` that proves `[SCENE] Set Source [G1_3]` lands in `zoneTracker._totals["Mud Burrow"]` (not `["G1_3"]`). Historical run files on disk that were saved with raw-id keys keep working (Load is unchanged); the user can `Reset PBs` or import-rebuild to regenerate clean PB keys from new runs.

- **INI-breaking characters in textual fields now rejected at import and on save.** Run files (`data/runs/{runId}.ini`) and the personal-bests INI rely on `\r`, `\n`, `[` and `]` having reserved structural meaning. Before this fix nothing stopped a hand-edited or maliciously crafted import payload from putting any of those characters into `profile`, `patch`, `firstTs`, `runId`, a zone-name key under `totals`, a detail row's `category`/`label`/`note`/`timestamp`, the PB `runPbRunId`, or a `zonePbs` zone key. A single `\n` would silently split a row across two INI lines on the next save, making the `[details].count` value mismatch the actual rows and corrupting `Load`. The fix is two layers: (1) `RunExportFormat.ValidateSchema` now scans every textual field on every run plus the PB block, and reports `"runs[N].fieldName contains INI-breaking character (\\n)"` so the user sees which field needs cleaning before the import touches disk; (2) `RunHistoryRepository._SerializeBuildResultToIni` defensively throws `ValueError` on the same character set, routed through the existing `WarningSink` so `Save` returns `false` and emits a warning identifying the failed `runId` — protection against a future parser regression smuggling bad text in through some other code path. Safe punctuation (`=`, `;`, `|`, parens, long strings, Unicode) is explicitly preserved by a round-trip test. 17 new tests: 9 covering each rejected field/character combination at the import layer (`run_export_format_tests.ahk`), 5 round-trip tests confirming safe characters survive Save/Load unchanged, and 3 defensive-throw tests confirming a bad field surfaces through the `WarningSink` instead of corrupting the file (`run_history_repository_tests.ahk`).

- **Removed accidental Exit hotkey (`Ctrl+Alt+Q`).** The entry script registered `^!q::ExitApp()` as a global hotkey. On ABNT2 and other international keyboards `AltGr+Q` is reported by Windows as `LCtrl+RAlt+Q`, which AHK's `^!` matches exactly — pressing AltGr+Q during a run closed the app and lost in-progress state. The tray menu "Exit" item already covers the use case, so the global hotkey was redundant and unsafe. Removed entirely; tray-menu Exit is the documented way to close the app.

- **`RunHistoryRepository.Save` is now atomic.** The previous implementation made roughly N direct `IniWrite` calls against `data/runs/{runId}.ini` (meta + totals + checkpoints + details, all sequential, each one a separate file open/modify/close round trip). A crash anywhere in that sequence — PC freeze, BSOD, mid-finalize close, antivirus quarantine grabbing the file — left a half-written run on disk: `[meta]` complete, `[totals]` missing, `[details]` count out of sync with the actual rows. The new implementation builds the full INI in memory and writes it through `AtomicWriter` (`.tmp` + `FileMove`), so the destination only ever sees a fully-formed file or no change at all. Same byte-compatible UTF-16 LE BOM output that `IniWrite` used to produce, so existing runs on disk and the `Load` path stay unchanged. The pure `_SerializeBuildResultToIni(buildResult, currentRunId, totalMs)` helper that builds the content is testable in isolation, and Save now also routes its failure path through the injected `WarningSink` instead of returning `true` regardless of what happened on disk. Two new tests cover the atomicity contract: no `.tmp` orphan after a successful save, and `Save` returns `false` + warns on writer failure.

- **Hydration ordering bug (RunStatsRecorder silently dropped hydrated runs).** `SpeedKalandraApp.__New` used to call `RunService.Hydrate` mid-construction. When the loaded state had an active run, `Hydrate` published `Evt.RunStarted{hydrated:true}` before `RunStatsRecorder` (and other downstream subscribers) had been created. Finalizing the hydrated run later produced a snapshot with `runId=""`, which `RunHistoryRepository.Save` silently rejected — the run was lost. `Hydrate` is now deferred to the end of `__New` after `_WireEventHandlers()`, and `ZoneTrackingService._OnRunStarted` respects the `hydrated:true` flag so the in-flight event no longer wipes the totals that were just restored from disk. Regression tests added in `zone_tracking_service_tests.ahk` and `speedkalandra_app_integration_tests.ahk`.
- **`LoadingTotalsService` wiped hydrated `_totalMs` on `RunStarted{hydrated:true}`.** Sibling of the hydration-ordering bug above: the composition root hydrates the loading total from disk via `loadingTotals.Hydrate(loadingMs)`, but `_handlerRunStarted` was hard-wired to `Reset()` and so the deferred `RunService.Hydrate()` event zeroed the value immediately after restoration. Loading time of an in-progress run was lost on every reload. Handler now goes through `_OnRunStarted(data)` which honours the `hydrated:true` flag, mirroring `ZoneTrackingService`. Regression tests `run_started_with_hydrated_flag_preserves_total_ms` and `run_started_without_hydrated_flag_wipes_total_ms` added in `loading_totals_service_tests.ahk`.
- **`LoadingDetectionService` false positives from non-game windows.** The default window provider matched the substring `"Path of Exile 2"`, which also matched Chrome tabs on the PoE2 wiki and Discord channels with that text. The HUD scanner then sampled pixels from the wrong window. Replaced with an `ahk_exe` scan over the same canonical list `FocusAutoPauseService` uses (`PathOfExile2Steam.exe`, `PathOfExile2_x64.exe`, etc.), and lock every follow-up `WinGetMinMax` / `WinGetPos` to the resolved HWND via `ahk_id`.
- **`UndoLastSave` left personal bests pointing at the deleted run.** Inconsistent with `RunHistoryDialog.Delete`, which already rebuilt PBs. `UndoLastSave` now invokes a new `_RebuildPbsFromHistory` helper after a successful delete (mirrors the dialog), so deleting the most-recent run via the tray immediately purges any PBs the deleted run contributed to.

### Changed

- **Simplicity audit + residual cleanup (Fase 11).** Qualitative review of the codebase against GSG §10's overengineering signals (formal class names, pass-through abstractions, abstractions without tests proving value, comments a better name would solve, patterns applied because "professional", `app.ahk` growing without organic extraction). The composition root, the four extracted collaborators (`BootPrompts`, `RunSnapshotSaver`, `RunStatePersister`, `LiveReconfigurationHandlers`) and every service were inspected; the conclusion is that the current shape pays for itself — each abstraction has a documented purpose and a test that exercises behaviour rather than constructor-only smoke. What this commit changes is the residue the Fase 12 strip missed: six files still carried historical phase/version/bug tags (`v17.13`, `v17.14`, `v17.15.3`, `v0.1.2 (audit #19)`, `v0.1.0 Phase 5`, `Phase 9.7`, `Phase 9.10`, `Item 1`, `Item 2`, `Smoke fix Turn 2`) in source comments — those are removed without changing behaviour, per GSG §11 (history belongs in the changelog, not the source). While in `steve_layout_widget.ahk` the audit also surfaced genuine dead code left over from the bar removal: the `COLOR_MAPA` / `COLOR_LOADING` / `COLOR_CIDADE`, `WHALE_IMG_PATH` / `WHALE_X` / `WHALE_Y` / `WHALE_W` / `WHALE_H` / `WHALE_GAP` static constants, the `_whaleLoaded` instance field, and the `BAR_TIMER_GAP` constant were declared but never read — the bar and the decorative whale image were both removed in earlier passes and left these declarations stranded. The `loadingTotals` constructor parameter (and the matching `_loadingTotals` field) on `SteveLayoutWidget` was stored at construction but never read either, so it was removed from both the signature and the call site in `app.ahk`; no tests exercised `SteveLayoutWidget` so no test changes were needed. Net diff: ~70 source-comment lines and ~15 dead declarations gone, zero behaviour change, suite stays at 1808/1808. The audit notes that `LiveReconfigurationHandlers` has formal-looking cohesion (three handlers grouped because they share `cfg`/`log`/`headless` constructor args rather than because they share a domain) but inlining it would require rewriting 16 unit tests against `SpeedKalandraApp` integration — cost beats benefit, the class stays. `EventBus` was reviewed and stays: the pre-publish-hook fix in Fase 2, the `_AssertWired` boot-time check, and the `EventTraceLogger` diagnostic all exist specifically because there is a bus, and removing it would re-introduce hard coupling at the composition root.

- **Tag-triggered release workflow + CI validation of the build script + hang-defence for the AHK suite (Fase 8).** Three pieces close the release pipeline: `.github/workflows/release.yml` (new) runs on pushed `v*` tags (and via `workflow_dispatch` against an existing tag), installs AutoHotkey v2 on a Windows runner, runs `tests_v2/build/test_build_dist.ps1` as a smoke check on the build script itself, then runs `build-dist.ps1 -Compile -Zip -Force` (which already runs the full AHK suite as a release gate — a red suite aborts packaging) and creates a **draft** release on GitHub with `SpeedKalandra-${tag}.zip` and `SpeedKalandra-${tag}.zip.sha256.txt` attached. The maintainer reviews the draft and publishes manually; auto-publish was rejected because a wrong tag would require force-pushing to recover. The workflow uses the `gh` CLI (preinstalled on GitHub-hosted runners) instead of a third-party action so the release pipeline does not add a supply-chain dependency. Both CI workflows pin AutoHotkey to version 2.0.19 explicitly (`choco install autohotkey --version=2.0.19`) instead of taking the latest from chocolatey — the latest at the time of writing (2.0.24) reproducibly hangs one integration test (`complete_run_flow_from_start_to_finalize_to_undo`) under non-interactive CI sessions with no error reaching `OnError`, stderr, or the log. The test passes locally on 2.0.19 and the rest of the suite passes on 2.0.24, so the failure is narrow and the pin keeps the CI signal honest until that one test can be isolated into a smaller reproducer; bumping the pin to 2.0.20+ is a tracked follow-up. `.github/workflows/test.yml` gains a `Validate build-dist script` step running `test_build_dist.ps1` on every push and PR — catches leakage regressions (a personal-data sentinel slipping past the filter into the zip), SHA256 sidecar format drift (the verification command in RELEASE.md would silently break for end users), and `-AhkPath` resolution regressions. Both workflows also gain explicit `timeout-minutes` caps on every long-running step (8 minutes on the AHK test suite, 3 minutes on the build-dist validator, 15 minutes on the release build, 25 minutes job-level cap): without these, a hung test could silently consume up to GitHub's 6-hour default, and the `Upload test log` step (`if: always()`) would never run because the prior step never ended — with the timeouts, a hang fails the step loud at a sane bound and the log artifact is still preserved for diagnosis. The test step also swaps `Start-Process -Wait` for a direct `&` call (the previous form can sit waiting on inherited handles even after the child process has exited — a known Windows-runner quirk that produced a 19-minute zero-output hang on the first CI build of these tests) and passes `/ErrorStdOut` so AHK parse errors hit stderr instead of a modal dialog that would itself hang the runner. `tests_v2/run_tests.ahk` installs an `OnError` handler **before any `#Include`** that captures runtime errors during global-scope code (a static initializer, a top-level setup line) which would otherwise pop a MsgBox and hang non-interactive sessions — the handler writes the error to a new `tests_v2/tests_boot.log` (separate from `tests_output.log` because `TestReporter.Init()` deletes the latter on entry) plus stderr, then `ExitApp(2)`. The boot log also receives a header on every run (AHK version + working dir + env state) and a `REG: ClassName` line for every successful `TestRegistry.Register` plus a `RUN:` / `OK:` pair around every test execution in `TestRunner._RunOne`, so a future hang can be diagnosed from the artifact alone — the last `RUN:` without a matching `OK:` names the test that killed the process. Both logs are uploaded as workflow artifacts via `if: always()`. `build-dist.ps1` gains a `-AhkPath` parameter with a new `Resolve-AhkPath` helper covering: explicit path (must exist; failure does NOT fall through to the discovery cascade, so a misconfigured CI runner fails loud instead of silently falling back to an arbitrary system AHK), standard install locations (`Program Files\AutoHotkey\v2\...`, x86 variants, `LOCALAPPDATA\Programs\...`), and `PATH` via `Get-Command`. `test_build_dist.ps1` gains a scenario B verifying that an explicit `-AhkPath` pointing at a non-existent file aborts the build before any side effect (no dest dir is created, exit code is non-zero); without this guard, a future "convenience" refactor that fell back to standard paths on explicit miss would silently re-introduce the CI-misconfiguration trap. New `RELEASE.md` documents the full release path: how to create a release (tag + push), the manual `workflow_dispatch` route, local-only build with `-SkipTests`, the three release gates (AHK suite green, `test_build_dist.ps1` green, AHK v2 installed), and a troubleshooting section covering the common failure modes.

- **Added smoke + algorithm test coverage for `RunStatsPlotDialog` and `CompactLayoutWidget` (Fase 10).** Both components had zero coverage despite being the two largest UI files in the project; smoke and algorithm tests now exercise everything that doesn't require a real `Gui`. `RunStatsPlotDialog` (26 tests in `tests_v2/unit/ui/run_stats_plot_dialog_tests.ahk`) covers constructor type-checks (5 deps), the static color helpers (`_PaletteAt` palette wrap-around and `_ColorForLabel` hash determinism, including the empty-label edge case), the headless `Open`/`Close` lifecycle through the `_ShowWithData` short-circuit that keeps `_gui=""`, the `_Segs*` aggregation family across all five granularities (`run`/`ato`/`mapa`/`cidade`/`loading` — with Mud Burrow re-entries aggregating by label and legacy Portuguese `Ato N` notes accepted alongside `Act N`), `_BuildLineChartSeries` line-break semantics (absent label → `present: false` so the chart breaks instead of dropping to zero), the min-act and profile filters in `_CollectRunsForChart`, `_GetAvailableProfiles` dedup + alphabetical sort, `_RoundUpYMax` thresholds (60s / 5min / 1h candidates, fallback for extreme values), `_FormatTimeShort` and `_ShortDateForLabel` formatting, and the per-granularity scoping of `_hiddenSeries` (a label hidden under "run" is not hidden under "mapa"). `CompactLayoutWidget` (18 tests in `tests_v2/unit/ui/compact_layout_widget_tests.ahk`) covers constructor smoke with minimal vs full deps, `_GetFixedSize` baseline (380×96 at scale 1.0), the static text-shrinking helpers (`_EstimateTextW` linearity in length + font size, `_SegmentLabel` thresholding empty/pct-only/name+pct), the pure instance helpers (`_FormatAct` em-dash fallback when act unknown, `_ResolveTimerColor` neutral/under-PB/over-PB color resolution), the bus handlers that mutate state without touching the Gui (`_OnDeathDetected` increments, `_OnRunRestart` zeroes on `RunStarted`/`RunReset`/`RunCancelled`, `_OnZoneEntered` mutates `_currentZone` + `_currentAct`), and the `Dispose` lifecycle that unsubscribes all 9 bus handlers. Test files use `headless=true` for the dialog (production constructor flag) and rely on `WidgetBase` already deferring Gui construction to `Show()` for the widget; nothing GDI-backed (`LineChartRenderer`) is exercised here, in line with the Fase 10 brief's "camada de builder testada sem depender de pixel-perfect screenshot".

- **Added test coverage for `EventTraceLogger` (opt-in diagnostic feature) at both unit and integration levels.** The component had zero tests despite being the canonical bus observer wired by `SpeedKalandraApp.__New` and conditionally started under `cfg.eventTracingEnabled`. New file `tests_v2/unit/app/services/event_trace_logger_tests.ahk` adds 18 unit tests covering: constructor type-checks (`bus` is `EventBus`, `log` has `Info`), initial-state contract (`IsEnabled()=false` and no interceptor on the bus until `Start()`), lifecycle idempotency (repeated `Start`/`Stop` don't stack or remove twice, `Stop` on a never-started tracer is safe), capture behaviour (publish with tracer active produces an `INFO [Event] EventName | key=value...` entry, payload Map serialized inline, tracer inactive produces nothing), and the static `FormatPayload` (Map/Array/long string truncation under `MAX_VALUE_LEN=200`, nested-map depth limit at `MAX_MAP_DEPTH=2`, empty payload returns `(none)`). 3 new integration tests in `speedkalandra_app_integration_tests.ahk` complete the picture: `event_tracer_start_registers_interceptor_on_app_bus` and `event_tracer_stop_removes_interceptor_from_app_bus` cover the production `EventTraceLogger` + `EventBus` composition wired by `__New`, and `event_tracer_active_captures_events_published_through_app_bus` proves end-to-end that a tracer attached to `app.bus` sees both the trigger command (`Commands.NewRunRequested`) AND the derived event (`Events.RunStarted`) produced by `RunService` reacting to that command. The existing `constructor_event_tracer_not_enabled_by_default` already covered the `cfg=false` half of the opt-in contract; the new tests complete the spec the Fase 7 roadmap item asked for (`eventTracingEnabled=false` → tracer doesn't start; `eventTracingEnabled=true` + Start → tracer captures; published event → log entry produced). The `app.Start()` branch that gates `tracer.Start()` on `cfg.eventTracingEnabled` is exercised structurally rather than by running the real Start (which would arm `SetTimer` callbacks and show widgets): unit tests prove the tracer activates on `Start()` and captures events, integration tests prove the production `eventTracer` instance is wired against the production `bus`, so the if-branch in `app.Start()` is the only piece left as trivial composition.

- **`RunExportFormat.ValidateSchema` now rejects negative or non-numeric values in optional numeric fields of an imported run.** Audit of the import path turned up four fields that the exporter always emits as valid non-negative integers, but that the validator accepted in any value the parser could coerce — hand-edited or maliciously crafted JSON could pass `deathCount: -3`, `maxActReached: -1`, `totals: {"Mud Burrow": -5000}`, or `details[].ms: -100` and all four would land in the saved `data/runs/{runId}.ini` and from there into the plot dialog, PB comparisons, and the run-history totals (a -3 in the death chart, distorted per-zone breakdowns, etc.). The schema gateway now rejects each with a clear error message before any disk write — same pattern the file already used for INI-breaking characters (`\r\n[]` in textual fields). Zero remains a legitimate value everywhere (zero deaths is the common case for a clean run, a zone visited for 0 ms happens when the player crosses an instance without the timer registering a measurable stay), so the lower bound is 0 not 1; only `totalMs` keeps its strict `> 0` requirement at the run level. 8 new unit tests in `run_export_format_tests.ahk` (6 rejections + 2 positive controls confirming zero and absent fields still validate). No changes to `RunImportService` itself — it routes through `ValidateSchema` already, so the new rejections surface in `Preview()` as `success=false` with the gateway's error message exactly like the existing INI-character rejections do. (`runId` is sanitized separately by `RunHistoryRepository._PathForRunId` via `RegExReplace(runId, "[^A-Za-z0-9_\-]", "_")`, so path-traversal via a crafted `runId` was already mitigated and required no change.)

- **`SpeedKalandraApp.Stop()` is now terminal.** After `Stop()` runs, calling `Start()` on the same instance throws `Error("SpeedKalandraApp.Start: instance was already stopped — create a new SpeedKalandraApp instead")` instead of attempting a half-reset that would re-arm `SetTimer` callbacks against the same closure references and call `Show()` on widget Gui handles the OS already tore down. Previously `Stop()` flipped `_started` back to `false` and `Start()` had no guard against the second call, so an accidental resurrection would corrupt state silently. The new `_stopped` field tracks the terminal flag; it is set even when `Stop()` runs on a never-Start()ed instance (there is no scenario where stopping the app should make starting it viable again). `Stop()` itself remains fully idempotent — calling it N times is safe. 2 new integration tests (`start_after_stop_throws`, `start_after_stop_throws_even_when_never_started`) lock the contract; the existing `stop_does_not_throw_when_never_started` and `stop_is_idempotent` tests carry refreshed comments describing the new terminal semantics.

- **Run snapshot save moved off the bus and onto `RunService` pre-publish hooks (`SetOnBeforeFinalize` / `SetOnBeforeCancel`).** The earlier wiring subscribed `RunSnapshotSaver.Save` to `Evt.RunCompleted` and `Evt.RunCancelled` and relied on the bus's FIFO Subscribe order in `SpeedKalandraApp.__New` to run Save before `ZoneTrackingService._OnRunEnded` and `RunStatsRecorder.Reset()` cleared the in-memory state. The contract was implicit, undocumented in the test suite, and easy to break by reordering constructors. `RunService.FinalizeRun()` / `CancelRun()` now invoke a settable callback after mutating timer/state and before publishing the lifecycle event — the composition root wires Save through `SetOnBeforeFinalize(() => snapshotSaver.Save("completed"))` and `SetOnBeforeCancel(() => snapshotSaver.Save("cancelled"))` at the end of `__New`. Subscription order on the bus is now irrelevant for the save flow. A throw inside the hook is caught and logged via the injected `LogService` (new optional 5th constructor parameter on `RunService`, defaults to `NullLogger`), so a misbehaving save can't block widgets and state-clearers from reacting to the lifecycle event. 11 new unit tests in `run_service_tests.ahk` (constructor with explicit log, setter type-checks, hook invocation order, hook-skip when idle, publish-survives-throw, warn on hook throw) plus 1 end-to-end integration guardrail (`cancelled_long_run_saves_to_history_with_zone_totals_intact`) that exercises the previously-uncovered code path: cancel a 5-minute run, assert the history file is written and contains the zone totals. Closes the GSG §14 anti-regression item "FIFO da bus como sutil dependência de ordem de wiring".

- **README and `KNOWN_ISSUES.md` corrected on two long-standing inaccuracies.** (1) The disclaimer claimed SpeedKalandra "does not send inputs", but `SpeedKalandraOnExitHandler` in `speedkalandra.ahk` issues `Send "{Blind}{Ctrl up}{Alt up}{Shift up}{LWin up}{RWin up}"` on script exit — a defensive keyup to release stuck modifiers if AHK terminates while a hotkey is held. The disclaimer now mentions it explicitly and clarifies that it's OS-level housekeeping, not directed at the game. (2) Both README's "Game language" section and `KNOWN_ISSUES.md` claimed zone detection, deaths, level changes and loading detection "work regardless of client language" — but the PoE2 `Client.txt` is translated together with the UI, and every parser in `LogMonitorService` matches English fragments (`[SCENE] Set Source`, `has been slain`, `is now level`, `Generating level N area X with seed`, `[WINDOW] Lost focus`). On a non-English client the parsers never fire. Both files now state clearly that SpeedKalandra targets the English client; only loading detection (pixel-based) is genuinely language-agnostic, and only auto-start/auto-finalize have user-configurable regex.

- **Stripped historical version/phase/bug tags from source comments (first pass).** Comments in `src_v2/core/log_service.ahk`, `src_v2/core/event_bus.ahk`, `src_v2/app/bus/events.ahk` and `src_v2/app/services/log_monitor_service.ahk` carried tags like `v17.15 (Bug #32)`, `(v0.1.4)`, `(Wave 3)`, `(Phase 9.2)`, `(R7)` and `(Fase 1)` that documented *when* a change happened during development. Those tags add noise without explaining anything actionable about the code today — the bug history lives in this file and in `git log`, so the source can keep just the technical "why". All 16 occurrences across the four files were reworded in place: the technical explanation stays, the tag disappears. No behaviour change, no test change (suite still 1721 PASS). Other files (services, repositories, UI) were spot-checked and look clean; if residue surfaces in a later read a second pass can sweep them.

- **`EventTraceLogger` is now opt-in.** New `[Diagnostics].EventTracingEnabled` INI flag (default `0`). When false, the bus interceptor is constructed but never registered, so a normal install never persists raw `Client.txt` lines into `speedkalandra.log`. Users who need event-level traces for a bug report flip the flag (see `CONTRIBUTING.md`). Documented in `AppSettings` and surfaced via `SettingsRepository._{Load,Save}Diagnostics`.
- **Silent `try` sweep in critical paths.** Failures in settings save, run-data flush (5 s tick + shutdown), PB update, act-checkpoint capture, hotkey rebind, log-monitor restart, hydrated-run discard/finalize, zone-totals clear, death penalty application, and disclaimer/setup persistence are now logged via `LogService.Warn` instead of being swallowed by bare `try`. Same treatment at the infrastructure layer (`RunStateRepository.SaveZoneTotals`, `RunHistoryRepository._EnsureDir`) via `OutputDebug`, mirroring the existing convention in `RunHistoryRepository.Delete`. Legitimate silent `try` in lifecycle teardown (`Stop`, `Dispose`, `Hide`) and cosmetic side effects (`TrayTip`, `log.Info`) are kept intentionally.
- **Boot-time modals extracted from `SpeedKalandraApp` into a new `BootPrompts` class.** The disclaimer dialog, the Client.txt setup dialog, and the hydrated-run resume/finalize/discard prompt — together ~360 lines — used to live as private methods on the composition root. They are now a thin coordinator class owning no state of its own, constructed in `__New` with a `persistFn` callback and references to the services it needs (`logMonitor`, `runService`, `timer`, `log`). `Start()` calls three public methods (`ShowDisclaimerIfNeeded`, `PromptLogFileSetupIfNeeded`, `PromptHydratedRun`) in sequence. Pure refactor — no behavior change. 10 unit tests cover the guard clauses (headless, already-acknowledged, valid existing path, missing/inactive run service). `src_v2/app/app.ahk` is now ~360 lines shorter as a result.
- **Run finalization + undo extracted into `RunSnapshotSaver`.** `_SaveRunSnapshot`, `UndoLastSave`, `_MarkUndoableSave`, `_RebuildPbsFromHistory`, `_ExpireUndoableSave` (and the `MIN_CANCELLED_SAVE_MS` constant, renamed to `MIN_SAVE_MS` to match its actual semantics) moved out of `SpeedKalandraApp` together with the `_lastSavedRunId` / `_undoTimerFn` state they own. The `RunCompleted` / `RunCancelled` subscriptions in `__New` now route through a late-bound callback (`(data) => this._snapshotSaver.Save(...)`) — the saver itself is constructed near the end of `__New` once every collaborator it depends on exists. `app.UndoLastSave()` remains as a one-line delegate so the tray callback in `speedkalandra.ahk` is unchanged. Pure refactor — no behavior change. 10 unit tests cover the threshold gate, undo state machine, and PB rebuild semantics.
- **Persistence flow extracted into `RunStatePersister`.** `_PersistRunData` (5 s tick), `_PersistRunDataFull` (final flush from `Stop()`), `_PersistSettings`, and `_ComputeTotalsHash` moved out of `SpeedKalandraApp`, together with the dirty-cache fields `_lastSavedLoadingTotal` / `_lastSavedZoneTotalsHash` that they own. `Start()` now schedules `() => this._persister.Tick()` instead of `() => this._PersistRunData()`. The `_persistFn` closure passed to widgets, dialogs, and `BootPrompts` routes through `this._persister.PersistSettings()` so every settings write goes through the same instance. `__New` primes the cache via `PrimeLoadingTotalCache` / `PrimeZoneTotalsCache` after hydration so the first tick doesn't redundantly rewrite the just-loaded state. Pure refactor — no behavior change. ~30 unit tests cover skip-cache, full-flush, cache priming, and reset semantics.
- **Hot-reload and destructive-action handlers extracted into `LiveReconfigurationHandlers`.** Three handlers — `_OnDeathApplyTimerPenalty` (timer penalty applied on `Evt.DeathDetected`), `_OnHotkeysChanged` (Stop + Hydrate + Start on the hotkey service), and `_OnResetPersonalBestsRequested` (confirmation dialog + reset) — moved out of `SpeedKalandraApp` into a small class constructed in `__New` with the collaborators each handler needs (`cfg`, `timer`, `hotkeyService`, `personalBest`, `log`). The subscriptions in `_WireEventHandlers` are now one-line delegates: `(data) => this._reconfig.ApplyDeathPenaltyToTimer(data)` etc. `_OnLogFilePathChanged` stays inline because it mutates `_logMonitorTimer` on the composition root and the extra plumbing isn't worth the size win. Pure refactor — no behavior change. 16 unit tests cover the guard clauses, payload-vs-cfg fallback, and the headless reset path.

### Build

- **`build-dist.ps1` runs the AHK test suite as a release gate.** A red tree no longer produces a distribution. Bypass with `-SkipTests` (only for local iteration; recursive call from `tests_v2/build/test_build_dist.ps1` also uses this flag).
- **`build-dist.ps1 -Zip` now writes a SHA256 sidecar.** The `.zip` is accompanied by `.zip.sha256.txt` in the coreutils `sha256sum` format (`<hex>  <filename>`). Verifiable on Linux/macOS with `sha256sum -c` or on Windows with `Get-FileHash`.
- **`tests_v2/build/test_build_dist.ps1` added.** PowerShell test that builds a fake project with personal-data sentinels (`speedkalandra.ini`, `Client.txt`, `data/runs/*.ini`, `debug/`, `BKP/`, `_LIXEIRA/`, dev-only markdown, etc.) under `%TEMP%`, runs `build-dist.ps1 -Zip -SkipTests`, then asserts (a) every sentinel is absent from both the staged dir and the produced `.zip`, (b) the must-keep files (`speedkalandra.ahk`, `data/zones.csv`) are present, (c) the SHA256 sidecar exists, parses, and matches the .zip. Designed to live alongside the AHK suite without being driven by it — the AHK runner is the wrong tool for testing PowerShell build behaviour.
- **`build-dist.ps1` rejects descendant destinations.** Running `.\build-dist.ps1 -DestDir ".\dist"` used to enter a recursive copy. Now both ancestor and descendant relationships between source and destination are rejected with a clear error.
- **CI reintroduced.** New `.github/workflows/test.yml` runs the `tests_v2/` suite on `windows-latest` for every push and PR; installs AutoHotkey v2 via Chocolatey, runs in headless mode (`SPEEDKALANDRA_TEST_NO_GUI=1`), and uploads `tests_output.log` as an artifact on failure. The previous attempt had been disabled because `#Warn All, MsgBox` in `run_tests.ahk` made the runner exit with a non-zero code even on all-green runs — routed to `OutputDebug` instead.

### Fixed

- **PB persistence failures no longer disguised as success.** Five mutations in `PersonalBestService` (`UpdateFromRun`, `Reset`, `LoadFromExternal`, `SetAsRunPb`, `RebuildFromHistory`) used to end with `try this._PersistToRepo()` — a bare silent try that swallowed both repository-returned-false (disk full, locked file, encoding error) and actual exceptions. The PB landed in memory but never reached `data/personal_bests.ini`, and the user saw nothing in `speedkalandra.log`. They now route through a new `_TryPersistOrWarn(context)` helper that surfaces both branches as visible `[PB]` WARNs while preserving the original behaviour of NOT breaking the finalize flow on persist failure. `_PersistToRepo` returns the bool from `_repo.Save` so the helper can distinguish "repo refused" from "repo threw".
- **`RunStateRepository.LoadZoneTotals` graded failure handling.** Used to silently return an empty Map on any `FileRead` failure, masking lock / permission / encoding errors that lose run-in-progress zone time. Now follows the graded taxonomy documented in `ARCHITECTURE.md` § 14: file missing is silent (expected new state); `FileRead` throws produces a `[RunState]` WARN with the exception; individual malformed lines are skipped silently (resilient to manual edits); a non-empty file where every line failed to parse produces a corrupt-file WARN.

### Changed

- **`WarningSink` introduced in `core/` for infra/services without a LogService dependency.** Repositories were logging Save / Delete / EnsureDir failures via `OutputDebug` only, which is invisible to the user without DbgView. The new `WarningSink` is a duck-typed observability channel (three implementations: `NullWarningSink`, `LogServiceWarningSink`, `InMemoryWarningSink`) injected into `PersonalBestRepository`, `RunStateRepository`, `RunHistoryRepository`, and `PersonalBestService` at composition-root construction. Each receives a tagged sink (`"PB"`, `"RunState"`, `"RunHistory"`) so a grep of `[PB]` / `[RunState]` / `[RunHistory]` in `speedkalandra.log` isolates failures by source layer. All `OutputDebug("X.Y failed: ...")` calls in those files were replaced with `this._warn.Warn(...)`. Default parameter is `""` so existing tests that construct repos with the legacy positional signature continue to pass (degrade to `NullWarningSink`). 41 new tests cover the sink contract, the persist-failure paths in `PersonalBestService` (with stub repos that return false / throw), and the graded `LoadZoneTotals` taxonomy.

### Documentation

- **`SECURITY.md` rewritten with explicit scope.** Replaces the minimal stub with a project-appropriate threat model (local-first, no network, no telemetry), enumerated in-scope categories (unexpected network activity, private data leakage, file writes outside the project directory, release packages containing personal files, malicious behaviour in distributed artifacts) and out-of-scope categories (incorrect timing, game-log format changes, AV false positives without reproducible unsafe behaviour, anything in forks). Disclosure preferences and the private channel are kept.
- **Issue templates added.** `.github/ISSUE_TEMPLATE/bug_report.md` and `.github/ISSUE_TEMPLATE/feature_request.md` plus a `config.yml` that disables blank issues and points security and known-limitations cases at the right docs. Bug template requests SpeedKalandra version, AHK version, Windows version, PoE2 client language, run state at the time, `EventTracingEnabled` flag, reproduction steps, expected vs actual, log snippet (with a privacy-review note), screenshot, and reproducibility category.

- **Error handling policy documented.** New § 14 in `ARCHITECTURE.md` formalizes the rule the codebase already follows for when `try` / `catch` should log on failure: *log where silence would hide data loss, broken state, or actionable failure*. Lists the four buckets every catch in the project falls into (logged via `LogService.Warn`; silent during lifecycle teardown; silent for cosmetic side effects; silent UI fallbacks that abort safely), with concrete examples and a decision tree for new code. Cross-referenced from § 13 (Conventions) and from `CONTRIBUTING.md`. One borderline silent catch found during the audit — `RunSnapshotSaver` reading the act-checkpoints map for the saved-run payload — was promoted to bucket (1) with an explicit `Warn` because a failure there would put an empty checkpoints map into the on-disk run file with no signal (data loss in disguise).

- **Historical version tags scrubbed from source comments.** Roughly 40 files across `src_v2/` and `tests_v2/` carried internal-timeline markers in their headers and inline comments — `Wave N`, `Phase A1` / `Phase B2.1` / `Phase 6 Option A` / `Phase 9.2`, `v17.x` and `v0.1.x` version tags, `Bug #N`, `POST-DEMOLITION VERSION:`, `(Item N)`, `(refactor R7)`, `(R11.1)` — all of which described how the code reached its current shape rather than what the code does today. They are gone. The few comments that genuinely explained _why_ (e.g. why `ConvertUtf16ToUtf8` was removed, why `_OnZoneEnteredForLevel` defends against re-entries to The Riverbank) were preserved but reworded without the version tag. Stale references to extracted symbols (`_SaveRunSnapshot` after the `RunSnapshotSaver` split) were updated. Bug IDs in test names (`bug9_*`, `bug21_*`, `W4.1`, `W5.1`, `W9.1`, `W9.2`) were intentionally kept — they are stable contracts with `tests_v2/REGRESSION-COVERAGE.md`.
- **Disclaimer rewritten and made canonical across all three surfaces.** The README, the in-app boot modal (`BootPrompts.ShowDisclaimerIfNeeded`), and `CONTRIBUTING.md` previously carried three different versions, several of them with self-deprecating phrasing about being a player rather than a developer, promises of slow support, and an explicit "I won't pretend I wrote the architecture" line. A single shorter version now lives in the README and is mirrored faithfully in the boot modal. The substantive points (AI-assisted development, anti-cheat / TOS posture, use-at-your-own-risk, GPL forks) are preserved; the apologetic framing is gone.
- **Hard-coded test counts replaced by stable phrasing.** README, `CONTRIBUTING.md`, `tests_v2/README.md`, and `ARCHITECTURE.md` previously stated `~1569` (and the per-layer breakdowns in two of those files disagreed numerically). All references switched to either `over 1500` or removed entirely; the per-layer tables now describe coverage by suite name rather than by count, so the docs no longer drift every time a test is added.
- **`ARCHITECTURE.md` updated for the `BootPrompts` and `RunSnapshotSaver` extractions.** §6 (composition root) describes the late-bound `(data) => this._snapshotSaver.Save(...)` subscription pattern; §11 (dialogs) lists the boot modals as living in `app/boot_prompts.ahk` instead of inline in `app.ahk`. §15 (test suite) drops the version-tagged coverage table.
- **`KNOWN_ISSUES.md` added.** Catalogues design constraints the user should be aware of before opening an issue: atomic-write window on Windows, loading-detection HUD position assumption, English-only regex defaults, no boss detection, untested Win32 paths, and the hand-edited zones catalog. Linked from the README.

### Tests

- +5 from the docs/opt-in/undo work: `AppSettings.defaults_event_tracing_disabled_by_default`, `AppSettings.from_map_reads_event_tracing_enabled`, `SettingsRepository.save_load_preserves_diagnostics_event_tracing`, `SpeedKalandraAppIntegration.constructor_event_tracer_not_enabled_by_default`, `SpeedKalandraAppIntegration.undo_last_save_rebuilds_pbs_from_history`.
- +4 from the hydration fix: `ZoneTrackingService.run_started_with_hydrated_flag_preserves_totals`, `ZoneTrackingService.run_started_without_hydrated_flag_wipes_totals`, `SpeedKalandraAppIntegration.hydrated_run_propagates_run_id_to_stats_recorder`, `SpeedKalandraAppIntegration.hydrated_run_finalize_saves_to_history`.
- +2 from the LoadingTotalsService hydration sibling: `LoadingTotalsService.run_started_with_hydrated_flag_preserves_total_ms`, `LoadingTotalsService.run_started_without_hydrated_flag_wipes_total_ms`.

## [v0.1.3] — 2026-05

### Added

- **Client.txt setup dialog on first boot.** When `cfg.logFile` is empty or points to a missing file, a modal dialog appears with a pre-filled Steam path and a Browse button. Cancel calls `ExitApp()` — the app refuses to run without a valid log path.
- **Death penalty applied to the live timer.** `TimerService.AddPenaltyMs(ms)` and an `_OnDeathApplyTimerPenalty` handler subscribed to `Evt.DeathDetected`. When `cfg.deathPenaltyEnabled` is on, the timer jumps forward by `cfg.deathPenaltyMs` (150_000 ms = 2 min 30 s by default) the moment a death is detected, so the user no longer sees an inconsistency between the overlay timer and the post-finalize plot. **Default off** — the per-death overhead varies a lot by player (waypoint vs portal vs nearest checkpoint), so the estimate stays opt-in.

### Changed

- **Settings UI cleanup.** Removed the unused `Patch` field; the value is still stored internally (`cfg.gamePatch = "Unknown"`) for back-compatibility with old saved runs. Fixed a visual bug where the Client.txt Edit auto-expanded vertically when the path was long.

### Tests

- 19 new tests (13 unit covering `TimerService.AddPenaltyMs`, 6 integration covering the death-penalty handler's guard paths).

## [v0.1.2]

### Fixed

- **Loading-detection timeouts no longer silently discarded** (Bug #5). When a loading screen exceeded `cfg.loadingVisualMaxMs` (default 90 s, slow PCs), `LoadingDetectionService` correctly detected the timeout but then dropped the event via a `durationMs > maxMs` filter in `_End`. The HUD-return event for that loading never reached the bus, so the run plot underestimated total loading time. Timeouts now publish with the real duration (no clamp).

### Changed

- **Duration formatting consolidated** (audit #19). Four near-identical copies of `FormatMs` were collapsed into a single `Duration.FormatMs` in `domain/values/duration.ahk`. All call sites updated.
- **Multi-line log entries are quieter** (audit #26). Stack-trace style entries now indent continuation lines instead of producing N separate timestamped rows.
- **Version visible in three UI surfaces** (audit #30): tray tooltip, Settings window title, run plot footer.

## [v0.1.1]

### Fixed

- **`TextEncoding.ConvertUtf16ToUtf8` and `MigrateIniToUtf8` removed** (Bug #2). The migration produced INIs with a UTF-8 BOM, and AHK v2's `IniRead(path, section, key, default)` silently returns the default on UTF-8 BOM files. Result: every key-based read after migration returned the fallback value — settings, run state, personal bests all looked freshly-installed. Reverted to UTF-16 LE BOM throughout. Documented in `text_encoding.ahk` and `ARCHITECTURE.md` §14.
- **"Lechtansi" bug: zone timer kept ticking during pause.** A `[SCENE]` line emitted by the game while alt-tabbed became a `ZoneChanged` event, which restarted `_startMs` in `ZoneTrackingService` even though `TimerPaused` had been observed earlier. Added a `_timerPaused` flag so `_OnZoneChanged` respects pause state.

## [v0.1.0] — first public release

Switched to public SemVer (`MAJOR.MINOR.PATCH`). Pre-1.0 signals "functional, evolving, no API-stability commitment" and pairs with the in-app AI-assistance disclaimer.

### Added

- **Export / import of run history** as JSON. `RunExportService` serializes runs (optionally with PBs, optionally anonymized); `RunImportService` previews + applies imports with conflict resolution by content signature. Two new dialogs (`ExportOptionsDialog`, `ImportPreviewDialog`).

### Removed

- Legacy `_LIXEIRA/` (deleted from gitignored tree) — campaign route system, step-based splits, replay engine, gem planner, build planner. Pre-rewrite paradigm; no migration path. Final wave of the demolition that started in pre-public v17.x development.

---

## Pre-release history (`v17.x`, internal)

The `v17.x` tags are from the pre-public iteration of the project, when the codebase was being incrementally rewritten under the paradigm now described in `ARCHITECTURE.md`. They have no SemVer mapping and are no longer present in source comments. The rewrite produced the layered architecture (`core/`, `domain/`, `infra/`, `app/`, `ui/`), the EventBus, the repository pattern around INI persistence, and the test framework — see `ARCHITECTURE.md` for the resulting design.
